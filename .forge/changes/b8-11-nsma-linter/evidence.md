# Evidence — b8-11-nsma-linter

<!-- Status: designed -->
<!-- Audit: B.8.11 (b8-11-nsma-linter) — PURE GOVERNANCE/DATA change. No
     external version pins. The verify-then-pin equivalent is the LIVE on-disk
     re-read of constitution-linter.sh:665-731, state-management.yaml, and the
     governance standards documented below (read 2026-06-03). All evidence is
     INTERNAL-FILE evidence (file:line, NOT URLs). Article III.4: every internal
     fact in design.md ADR-B811-001..004 traces to a P-NN row here. -->

**Collection timestamp**: 2026-06-03 (on-disk re-read by the main orchestration
thread; authoritative). All facts are file-state reads of the working tree at
`/Users/bfontaine/git/github/forge` on `main`.

**Re-verify obligation**: the `ci_blocking` flag, forbidden list, and
`EXCLUDE_STANDARDS` interlock are re-read LIVE at `/forge:implement` before any
file is edited (b8-coroot lesson). If `constitution-linter.sh` has been refactored
in the interim, re-validate the fail/warn branch before flipping the YAML.

---

## Provenance Table (internal-file evidence — file:line, not URLs)

| ID | Source (file:line) | Read | What it proves |
|----|--------------------|------|----------------|
| P-01 | `.forge/scripts/constitution-linter.sh:665-731` | 2026-06-03 | Full NSMA rule block. Reads `state-management.yaml`, extracts `ci_blocking` flag at L683 (`grep -qE '^[[:space:]]+ci_blocking:[[:space:]]+true'`), extracts the `forbidden:` list via awk (L686-696), finds pubspec.yaml files excluding `/.forge/`, `/examples/`, `/.dart_tool/` (L701-705), and branches FAIL vs WARN per-violation at L715-719. The rule ALREADY EXISTS and runs today. B.8.11 is a DATA flip, not a code change. |
| P-02 | `.forge/scripts/constitution-linter.sh:683` | 2026-06-03 | `if grep -qE '^[[:space:]]+ci_blocking:[[:space:]]+true' "$nsma_yaml"; then nsma_blocking=1; fi` — the exact grep that reads the `ci_blocking` value. Flipping `state-management.yaml::enforcement.ci_blocking` from `false` to `true` is sufficient to activate blocking. No bash edit required. |
| P-03 | `.forge/scripts/constitution-linter.sh:715-719` (FAIL/WARN branch) | 2026-06-03 | `if [ "$nsma_blocking" = "1" ]; then fail "  forbidden state-mgmt dep '${pkg}' in ${pubspec#$FORGE_ROOT/} (no-state-management-alternatives, ci_blocking=true)"; else warn "  forbidden state-mgmt dep '${pkg}' in ${pubspec#$FORGE_ROOT/} (no-state-management-alternatives, warn-only ; ci_blocking flips at B.8/T6)"; fi` — verbatim. The branch is keyed entirely on `nsma_blocking`. |
| P-04 | `.forge/scripts/constitution-linter.sh:724-726` (PASS line) | 2026-06-03 | `pass "  no forbidden state-mgmt deps detected across $nsma_files_scanned pubspec.yaml file(s)"` — emitted when `nsma_violations = 0`. This is the expected output for the clean-pubspec L2 fixture assertion (FR-B811-041). |
| P-05 | `.forge/scripts/constitution-linter.sh:701-705` (scan exclusions) | 2026-06-03 | `find "$nsma_search_root" -type f -name pubspec.yaml 2>/dev/null \| grep -v "/.forge/" \| grep -v "/examples/" \| grep -v "/.dart_tool/" \|\| true` — the structural exclusion preventing /.forge/ templates and /examples/ from being scanned. Backward-compat by construction (FR-B811-030/031). |
| P-06 | `.forge/scripts/constitution-linter.sh:681` | 2026-06-03 | `nsma_search_root="${FORGE_LINTER_FIXTURE_ROOT:-$FORGE_ROOT}"` — the fixture override mechanism. When `FORGE_LINTER_FIXTURE_ROOT` is set, the NSMA scan operates on the fixture directory instead of the real Forge root. This is the L2 harness injection point (FR-B811-054). |
| P-07 | `.forge/standards/state-management.yaml:1-38` | 2026-06-03 | Full file. `version: "1.0.0"` (L6), `last_reviewed: 2026-05-04` (L7), `expires_at: never` (L8), `exception_constitutional: true` (L9), `linter_rule: no-state-management-alternatives` (L10), `enforcement.ci_blocking: false` (L12), `enforcement.pre_commit_hook: false` (L13), `enforcement.activation_planned: "B.8 (T6)"` (L14). These are the flip targets: L12 (false→true), L14 (remove, replace with `activated_by`), L6 (1.0.0→1.1.0), L7 (date update). |
| P-08 | `.forge/standards/state-management.yaml:15-23` (forbidden list) | 2026-06-03 | `forbidden:` block contains exactly 8 packages: `flutter_riverpod`, `riverpod`, `provider`, `get`, `getx`, `mobx`, `flutter_mobx`, `states_rebuilder`. This list is CLOSED — B.8.11 does NOT add, remove, or modify any entry (FR-B811-003). |
| P-09 | `.forge/standards/state-management.yaml:8-9` (structural-exception pair) | 2026-06-03 | `expires_at: never` (L8) and `exception_constitutional: true` (L9) co-exist. FR-J7-020 bidirectional guard: both fields must survive the version bump unchanged (FR-B811-004). |
| P-10 | `.forge/standards/state-management.yaml:32-38` (flutter block) | 2026-06-03 | `flutter:` block: `standard: flutter_bloc`, `version_pinned: ^9.0.0`, `companions: [bloc_test, hydrated_bloc, replay_bloc]`. BYTE-UNCHANGED by B.8.11 (FR-B811-003). |
| P-11 | `.forge/constitution.md:223-227` (Article VI.3 verbatim) | 2026-06-03 | "State management SHALL use `flutter_bloc` exclusively:\n- **Cubit**: For simple, synchronous state with no complex event processing.\n- **Bloc**: For complex state with distinct events and multiple transitions.\n\nNo other state management library (Provider, Riverpod, GetX, MobX, etc.) is permitted without explicit constitutional amendment." — the constitutional mandate B.8.11 activates enforcement for. |
| P-12 | `docs/ARCHITECTURE-TARGET.md:376-397` (ADR-006) | 2026-06-03 | ADR-006 ratifies the state-management discipline: `flutter_bloc` is the sole standard, alternatives are "bloqué par linter CI" (blocked by CI linter). The `state-management.yaml` `linter_rule: no-state-management-alternatives` exists specifically to enforce ADR-006. `ci_blocking: true` is prescribed as the target. The WARN-only deferral (`activation_planned: "B.8 (T6)"`) was a temporary Q-001 Option A at v0.4.0-rc.1. B.8.11 is the scheduled activation. |
| P-13 | `.forge/standards/global/linting-rules.md:173-190` (§"Adding a new rule") | 2026-06-03 | Full §"Adding a new rule" text, including: "A rule MUST NOT be tightened (lower threshold, stricter heuristic) without going through the same process." The §1-4 process requires: (1) Constitution amendment, (2) F.x change, (3) backward-compat audit, (4) update of this standard. B.8.11 is the F.x change (§2), includes backward-compat audit (§3), and updates the standard (§4). Whether §1 is satisfied by VI.3 + ADR-006 is the Q-001 question (ADR-B811-001, reviewer-adjudicated). |
| P-14 | `.forge/standards/global/linting-rules.md:160-166` (opt-out matrix) | 2026-06-03 | Table of `FORGE_LINTER_SKIP_*` env vars: `FORGE_LINTER_SKIP_V_1`, `FORGE_LINTER_SKIP_X_3`, `FORGE_LINTER_SKIP_XI_3`, `FORGE_LINTER_SKIP_XI_5`. B.8.11 appends a `FORGE_LINTER_SKIP_NSMA=1` row (FR-B811-021). The existing `FORGE_LINTER_SKIP_NSMA=1` is already consumed by the linter at L674 (P-15). |
| P-15 | `.forge/scripts/constitution-linter.sh:674-675` (NSMA opt-out check) | 2026-06-03 | `if [ "${FORGE_LINTER_SKIP_NSMA:-0}" = "1" ]; then not_applicable "  skipped via FORGE_LINTER_SKIP_NSMA"; else ...` — the opt-out is already implemented in the linter. The linting-rules.md row (FR-B811-021) documents it in the governance standard. |
| P-16 | `.forge/scripts/constitution-linter.sh:732-734` (I.3 interlock boundary) | 2026-06-03 | `# ── ADR-I3-001: T3-Forbidden Components — generic forbidden discovery ──` header immediately follows the NSMA block (L732). The NSMA rule (L665-731) and the T3-Forbidden rule (L732+) are distinct sections. The T3-Forbidden walk uses `EXCLUDE_STANDARDS` to hard-exclude `state-management.yaml` from its YAML-standard discovery scan, preventing double-fire. B.8.11 MUST NOT disturb this boundary. |
| P-17 | `.forge/scripts/tests/i3.test.sh:1-80` (harness pattern) | 2026-06-03 | `#!/usr/bin/env bash`, `--level` flag parse loop (L22-27), `HARNESS_DIR` resolution (L29), `source "$HARNESS_DIR/_helpers.sh"` (L43), `PASS=0; FAIL=0; FAIL_NAMES=()` counters (L44-46), named test functions, `run_test`/`print_summary`. The structural model for `b8-11.test.sh` (FR-B811-051). L2 fixture pattern: `FORGE_LINTER_FIXTURE_ROOT` env var gates fixture invocations. |
| P-18 | `.forge/standards/REVIEW.md:51-58` (transport.yaml 1.1.0 row format, t5-connect-codegen) | 2026-06-03 (re-confirmed at `/forge:implement`) | **LOW-2 corrected precedent.** The CORRECT version-bump precedent is `transport.yaml v1.0.0 → v1.1.0` (t5-connect-codegen), NOT identity.yaml — identity.yaml is still at v1.0.0. H2 format: `## 2026-05-05 — Updated transport.yaml to v1.1.0 (t5-connect-codegen)`; table row at L57-58: `| transport.yaml | 1.1.0 | KEEP-WITH-CHANGES | never (structural) | Added codegen.connect_layout_version ... |`. The shape B.8.11's REVIEW.md row follows (FR-B811-012): first cell = basename, second cell = `1.1.0`, decision `KEEP-WITH-CHANGES`, next review `never (structural)`. Matches the J.7 FR-J7-023 row regex (P-30). |
| P-19 | `.forge/schemas/standard.schema.json:38-47` (enforcement.additionalProperties) | 2026-06-03 | `"enforcement": { "type": "object", "additionalProperties": true, "required": ["ci_blocking", "pre_commit_hook"], ... }` — `additionalProperties: true` on the `enforcement` object. The schema comment at the same block: "additionalProperties:true accommodates documented extensions (e.g. state-management.yaml's `activation_planned: \"B.8 (T6)\"`)". An `activated_by:` sibling to `ci_blocking`/`pre_commit_hook` is schema-legal under this relaxed constraint (ADR-B811-004). |
| P-20 | `.forge/scripts/tests/i3.test.sh:67-68` (L2 fixture FORGE_LINTER_FIXTURE_ROOT) | 2026-06-03 | `MANIFEST: _test_i3_l2_t3_pubspec — FR-I3-T3F-062 / 120 / scenario 1` — the L2 fixture test uses `FORGE_LINTER_FIXTURE_ROOT` to redirect the linter to a synthetic tree. The same mechanism is available for b8-11's L2 tests (P-06). |
| P-21 | `find . -name pubspec.yaml \| grep -v "/.forge/" \| grep -v "/examples/" \| grep -v "/.dart_tool/"` → zero results | 2026-06-03 (re-confirmed at `/forge:implement`) | **Zero SCANNABLE pubspec.yaml after `/.forge/` + `/examples/` exclusions** (2 real pubspec.yaml files exist under `examples/` — `examples/forge-fsm-example/frontend/pubspec.yaml` and `cli/assets/examples/forge-fsm-example/frontend/pubspec.yaml` — both excluded by the structural `grep -v "/examples/"` filter; they never reach the NSMA scan; both are `flutter_bloc: ^9.0.0`-only, no forbidden pkg even if scanned). After the exclusions the scannable set is empty, so the NSMA scan with `ci_blocking: true` finds no pubspec.yaml to scan and the rule emits `not_applicable "  no Flutter pubspec.yaml found under $nsma_search_root"` (see P-01 L706-707: `if [ -z "$nsma_pubspecs" ]; then not_applicable ...`). The live tree is backward-compat GREEN by construction — no forbidden dep can fail. This confirms FR-B811-030 (OVERALL PASS preserved) and FR-B811-057 (live-tree backward-compat assertion). |
| P-22 | `.forge/standards/state-management.yaml:6` (`version: "1.0.0"`) | 2026-06-03 | The current version. B.8.11 bumps to `"1.1.0"` (ADR-B811-003). The bump is additive minor: only enforcement fields change; `forbidden:`, `flutter:`, `linter_rule:`, `rationale:`, and structural-exception pair are byte-unchanged. |
| P-23 | `.forge/standards/REVIEW.md:30-34` (state-management.yaml seed row) | 2026-06-03 | `| state-management.yaml | 1.0.0 | KEEP | never (structural) | Structural exception per ADR-006 |` — the original T.4 seed row. B.8.11 appends a new H2 entry with version `1.1.0` and decision `KEEP-WITH-CHANGES` (FR-B811-012, J.7 FR-J7-023). |
| P-24 | `.forge/scripts/tests/i3.test.sh:107` (EXCLUDE_STANDARDS interlock — indirect) | 2026-06-03 | `i3.test.sh` L2 fixture helper passes `FORGE_LINTER_SKIP_NSMA=1` when running the T3-Forbidden section, confirming NSMA is a distinct section and that the I.3 tests deliberately skip NSMA to avoid double-fire. B.8.11 must not disturb this coupling (FR-B811-042). |
| P-25 | `.forge/changes/b8-10-migrate-flagship/evidence.md:P-25` (change.schema.json) | 2026-06-03 | `status` enum includes `designed`; `timeline.properties` includes `designed`; `timeline.additionalProperties: false`. The `.forge.yaml` flip to `status: designed` + `timeline.designed: 2026-06-03` is schema-compliant. |
| P-26 | `.forge/scripts/constitution-linter.sh:674,681,683,715-719,732,782,821` (implement-phase LIVE re-read) | 2026-06-03 (`/forge:implement`) | **Phase 0 verify-then-pin LIVE re-read (b8-coroot lesson).** Confirmed UNCHANGED from P-01..P-06/P-15/P-16: L674 `FORGE_LINTER_SKIP_NSMA` opt-out present; L681 `nsma_search_root="${FORGE_LINTER_FIXTURE_ROOT:-$FORGE_ROOT}"` fixture override present; L683 `grep -qE '^[[:space:]]+ci_blocking:[[:space:]]+true' "$nsma_yaml"` → `nsma_blocking=1` present; L715-719 FAIL/WARN branch keyed entirely on `nsma_blocking` (NO refactor — no bash change needed for the flip); L732 `ADR-I3-001: T3-Forbidden Components` header immediately follows NSMA block; L782 `EXCLUDE_STANDARDS = {"state-management.yaml"}` + L821 `if relpath in EXCLUDE_STANDARDS:` — I.3 interlock intact, `state-management.yaml` still excluded from the T3-Forbidden Python walk. No `[NEEDS CLARIFICATION]` — all pins hold. |
| P-27 | `.forge/standards/state-management.yaml:6-14` (implement-phase LIVE re-read) | 2026-06-03 (`/forge:implement`) | **Phase 0 flip-target pins LIVE.** Pre-flip state confirmed: `version: "1.0.0"` (L6), `last_reviewed: 2026-05-04` (L7), `expires_at: never` (L8), `exception_constitutional: true` (L9), `enforcement.ci_blocking: false` (L12), `enforcement.pre_commit_hook: false` (L13), `enforcement.activation_planned: "B.8 (T6)"` (L14). Forbidden list still 8 pkgs (L16-23). All match P-07/P-08/P-22. No drift. |
| P-28 | `.forge/schemas/standard.schema.json:38-46` (implement-phase LIVE re-read) | 2026-06-03 (`/forge:implement`) | `enforcement.additionalProperties: true` (L40) confirmed UNCHANGED — `activated_by:` sibling field is schema-legal (ADR-B811-004). NOT flipped to `false`. |
| P-29 | `.github/workflows/forge-ci.yml:114-117` (implement-phase LIVE re-read) | 2026-06-03 (`/forge:implement`) | Insertion point confirmed: `"b8-10.test.sh --level 1"` at L115; `"b8o.test.sh --level 1"` at L116 closes the b8-N sequence before `)` at L117. `b8-11.test.sh --level 1` appends after L115 (matrix `harnesses=()` array format). No concurrent b8-11 entry present. |
| P-30 | `bin/validate-standards-yaml.sh:319-328` (implement-phase LIVE re-read) | 2026-06-03 (`/forge:implement`) | FR-J7-023 REVIEW.md drift check: row regex is `\|\s*<basename>\s*\|\s*<version>\s*\|` (L324) — the REVIEW.md table row MUST carry `state-management.yaml` and `1.1.0` as the first two pipe-delimited cells. The transport.yaml v1.1.0 row at REVIEW.md:51-58 (LOW-2 precedent) matches this shape. FR-J7-020 bidirectional `expires_at: never` ⇔ `exception_constitutional: true` enforced at L295-298. |

---

## Backward-Compat Finding (F.4 §3 — Article III.4 obligation)

**Finding (read 2026-06-03, re-confirmed at `/forge:implement`)**: `find . -name pubspec.yaml | grep -v "/.forge/" | grep -v "/examples/" | grep -v "/.dart_tool/"` returns **zero SCANNABLE results** in the Forge repo working tree. Two real `pubspec.yaml` files DO exist under `examples/` (`examples/forge-fsm-example/frontend/pubspec.yaml` and `cli/assets/examples/forge-fsm-example/frontend/pubspec.yaml`), but both are excluded by the structural `grep -v "/examples/"` filter and never reach the NSMA scan; both declare `flutter_bloc: ^9.0.0` only (no forbidden state-mgmt pkg even if they were scanned). After the `/.forge/` + `/examples/` + `/.dart_tool/` exclusions the scannable set is empty; the NSMA scan (`nsma_pubspecs` variable, P-01 L701-705) is empty; the rule emits `not_applicable "  no Flutter pubspec.yaml found under $nsma_search_root"` (P-01 L706-707) and the NSMA section exits cleanly.

**Consequence**: flipping `ci_blocking: false → true` (P-07) produces **zero new FAIL lines** against the live tree. OVERALL PASS is preserved. This satisfies FR-B811-030 (live-tree OVERALL PASS), FR-B811-031 (no archived change retroactively fails — /.forge/ is excluded structurally, P-05), and FR-B811-032 (harness backward-compat assertion).

**Confirmed exclusions** (P-05):
- `.forge/**` pubspec.yaml files (templates, archived changes) — excluded via `grep -v "/.forge/"`
- `/examples/**` pubspec.yaml files — excluded via `grep -v "/examples/"`
- `/.dart_tool/**` — excluded via `grep -v "/.dart_tool/"`

No scannable forbidden dep exists anywhere outside these exclusions in the current working tree.

---

## Anti-Hallucination Pass (Evidence Phase)

- **Rule block P-01..P-06 are quoted verbatim** from the live `constitution-linter.sh`. No linter internal is fabricated beyond what the P-NN rows quote.
- **Forbidden list P-08 is closed at 8 packages**. No new package is invented. The list is re-read from the live `state-management.yaml`.
- **Schema legality of `activated_by` (P-19) is a HARD FACT**: `enforcement.additionalProperties: true` in `standard.schema.json` is read verbatim. The schema comment explicitly names `activation_planned` as an example of the accommodated extension pattern.
- **Zero scannable pubspec.yaml (P-21) is a HARD FACT**: `find` command run live; zero output. The NSMA section will emit `not_applicable`, not FAIL.
- **Q-001 (F.4 §1 amendment) is NOT self-ruled here**. The provenance rows establish the grounds for the no-amendment lean (P-11 VI.3 mandate + P-12 ADR-006 + P-13 §1-4 process) but the adjudication is reserved for the independent reviewer (ADR-B811-001, RATIFICATION-PENDING).
