name: b8-13-rollback-runbook
status: archived
created: 2026-06-04
schema: default
constitution_version: "1.1.0"
parent_audit_items:
  - B.8.13  # docs/new-archetypes-plan.md §4.2 B.8.13 (lines 2339-2344) — "Critères de rollback documentés (ARCHITECTURE-TARGET §11.3): p99 augmente >20% après Envoy → rollback Kong ; Erreurs traceparent >1% → rollback OTel SDK seul ; ~~DBOS Postgres saturé >70% CPU → fallback Temporal~~ REMOVED per B8O. Effort S." GROUND-TRUTH NOTES (CLAUDE.md ANTI-HALLUCINATION PROTOCOL ; III.4 = "Ambiguity Protocol" — citation corrected after independent review): (1) ARCHITECTURE-TARGET §11.3 ALREADY EXISTS (docs/ARCHITECTURE-TARGET.md:856-861) and STILL CARRIES the stale DBOS-CPU criterion at line 860 ("DBOS Postgres saturé > 70 % CPU → fallback à Temporal pour workflows lourds.") — B8O (b8-orchestration-temporal-realign, archived 2026-06-01, commit 56a8b7d) realigned orchestration.yaml + temporal.md to Temporal-as-default but LEFT ARCHITECTURE-TARGET untouched, so §11+§12.1 are B8O-stale in SEVEN spots: §11.1 mermaid :805 "DBOS embedded", §11.2 Phase-2 :832 "Migrer Temporal → DBOS", §11.2 Phase-2 risk :837 "DBOS Go SDK encore récent", §11.2 Phase-3 risk :847 "maturité DBOS pour AI agents", §11.3 :860 DBOS-CPU criterion, §11.4 risk :867 "DBOS Go SDK breaking changes", §12.1 :930 "default: dbos". DESIGN ROUND-2 (ADR-B813-002, post-independent-review): the arch doc is sha256-PINNED (t4.test.sh::_test_t4_023) and the DBOS realignment is a MATERIAL edit (rehash hatch forbids material edits) ⇒ B.8.13 does NOT edit it. Instead docs/ROLLBACK.md becomes authoritative and carries a Supersession note recording all seven refs as obsolete-per-B8O (pointing at orchestration.yaml v1.2.0) — the same record-only pattern B8O used. (2) The runbook is a NEW deliverable — docs/MIGRATIONS.md:144 already forward-references it verbatim ("Rollback criteria — see B.8.13 for the full runbook"); :172/:182 reference "B.8.13+"/"B.8.13 rollback thresholds". B.8.13 lands it in a dedicated docs/ROLLBACK.md (operational detect→decide→execute→verify→re-attempt) and re-points the MIGRATIONS.md forward-reference at it (MIGRATIONS.md is NOT sha-pinned). (3) The two surviving criteria are RELATIVE thresholds (p99 +>20% post-Envoy → roll back the Kong→Envoy route weights ; traceparent errors >1% → roll back the OTel SDK overlay only) — NO absolute p50/p95/p99 number may be committed (ADR-B8-1-002 + CLAUDE.md ANTI-HALLUCINATION PROTOCOL ; fsm-backend ships image: scratch/whoami placeholder, B8-BASELINE §3). The runbook documents the measurement procedure (B.8.12 methodology, docs/B8-BASELINE.md §6) and the relative deltas only, and stays byte-consistent with the criteria embedded in forge-migrate-flagship.sh:394-396 (FR-B813-032). (4) The rollback MECHANISMS already exist and are NOT (re)built here: full-tree restore = bin/forge-migrate-flagship.sh --target . --rollback (B.8.10, restores the B.8.2 byte-frozen 1.0.0 snapshot) ; Kong rollback = reverse the additive canary route cutover (Envoy ∥ Kong, B.8.4 ; MIGRATIONS.md:106 "Canary cutover — Kong → Envoy, by route") ; OTel-SDK rollback = revert the SDK overlay only (not a full rollback). The runbook documents how to invoke them, not new code. (5) Pure doc + harness brick — NO standard bump, NO schema mutation, NO constitution mutation (Article VIII.1/VIII.2 amendment + scaffoldable flip are B.8.14), NO snapshot rebuild ; constitution_version unchanged ; frozen 1.0.0 templates byte-identical (b8-2 sha256 guard).
depends_on:
  - B.8.12  # b8-12-e2e-migration — the latency-measurement methodology (docs/MIGRATIONS.md "Latency measurement methodology" + docs/B8-BASELINE.md §6) + span-inventory before/after gate the runbook's detection step builds on ; the two relative thresholds the runbook operationalises were ratified here (no committed p99)
  - B.8.10  # bin/forge-migrate-flagship.sh --rollback (full byte-frozen 1.0.0 restore) + docs/MIGRATIONS.md:144 forward-reference ("see B.8.13 for the full runbook") this brick lands ; the migrate driver is the full-tree rollback mechanism the runbook invokes
  - B.8.4   # b8-4-envoy-gateway — Envoy ∥ Kong additive canary cutover (HTTPRoute/route weights) ; "roll back Kong" = reverse this route cutover, the runbook's Scenario A execution step
  - B.8.2   # frozen 1.0.0 snapshot (.forge/scaffold-snapshots/full-stack-monorepo/1.0.0.tar.gz + .sha256, b8-2.test.sh guard) — the byte-frozen full-tree rollback target --rollback restores from
  - B8O     # b8-orchestration-temporal-realign (archived 2026-06-01) — Temporal IS the orchestrator (no DBOS Rust SDK ; ADR-B8O-001 ; orchestration.yaml v1.2.0 default_by_language.rust: temporal) ; the DBOS-CPU rollback criterion is moot (no DBOS leg to roll back to/from). DESIGN ROUND-2 DECISION (ADR-B813-002): record-only supersession — B8O itself deliberately did NOT edit the pinned ARCHITECTURE-TARGET.md (b8-orchestration-temporal-realign/design.md:18) ; B.8.13 follows that precedent.
  - T.4     # t4-adr-ratification — docs/ARCHITECTURE-TARGET.md is sha256-PINNED by t4.test.sh::_test_t4_023 (:278-294) against the hash in t4-adr-ratification/specs.md (live hash 502bd8a2…895d == pinned). INDEPENDENT-REVIEW BLOCKER: editing §11 in place flips that pin RED on main CI ; the bin/forge-rehash-architecture-doc.sh hatch is non-material-only ("Material edits MUST be ratified by a fresh Forge change"). ⇒ B.8.13 does NOT edit the arch doc ; harness positively asserts its sha256 is unchanged + adds t4 to the coupling guard (FR-B813-040/068).
timeline:
  proposed: 2026-06-04
  specified: 2026-06-04
  designed: 2026-06-04
  planned: 2026-06-04
  implemented: 2026-06-04
  archived: 2026-06-04
