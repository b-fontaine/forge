{
  "_audit": [
    "Audit: B.9.2 (b9-2-web-pwa, FR-B9-2-010/013/014)",
    "Standards: .forge/standards/web-frontend.yaml (framework) + pwa.yaml (PWA capability)",
    "NEVER PUT SECRETS HERE. VAPID keys are an ADOPTER responsibility (see README).",
    "Pins verify-then-pin LIVE 2026-07-28 (evidence.md P-1/P-2).",
    "oauth4webapi re-resolved LIVE 2026-07-31 (b9-3 evidence.md P-3): 3.8.6 is the",
    "npm latest, MIT, and installs ZERO transitive packages ('added 1 package').",
    "vite is pinned EXACTLY: vite 8.x is the npm latest (8.1.5) but is EXCLUDED by",
    "@builder.io/qwik 1.20.0 peerDependencies '>=5 <8' — re-confirmed live 2026-07-28.",
    "NO @connectrpc/* dependency: mobile-pwa-first is layer_profile client-only and",
    "has NO backend layer (ADR-B9-1-001 / ADR-B9-2-001).",
    "WORKAROUND (t5-qwik-cli-ignore-dep, 2026-09-09): `ignore` is a devDependency",
    "we do not import. @builder.io/qwik 1.20.0 publishes dist/cli.cjs which",
    "require(\"ignore\") at module-init while declaring only csstype/launch-editor/",
    "rollup, so WITHOUT this entry every qwik subcommand dies MODULE_NOT_FOUND —",
    "`qwik --help` included, not just `qwik build`. 1.20.0 is the npm latest, so",
    "there is no fixed release to bump to. Pinned by web-frontend.yaml v1.2.0.",
    "DELETE this entry once upstream declares `ignore` itself. RE-CHECKED LIVE",
    "2026-09-13 (t7-qwik-deps-refresh): @builder.io/qwik 1.20.0 is still npm latest",
    "and still declares only csstype/launch-editor/rollup, so the entry stays.",
    "",
    "PINS RE-RESOLVED LIVE 2026-09-13 (t7-qwik-deps-refresh), verified by",
    "npm install + npm audit + tsc --noEmit + qwik build on a real render:",
    "  typescript          ^5.4.0  -> ^7.0.2   (tsc 7.0.2 typechecks the surface clean)",
    "  @types/node         ^22.0.0 -> ^24.13.4 (tracks .nvmrc's node 24 — NOT npm",
    "                      latest 26.5.1, which would not match the pinned runtime)",
    "  vite-tsconfig-paths ^4.2.1  -> ^6.1.1",
    "  vite                HELD at =7.3.6 — qwik 1.20.0 peerDependencies is still",
    "                      \">=5 <8\", re-confirmed live; vite 8.3.0 is npm latest",
    "                      and still excluded.",
    "  @builder.io/qwik*, oauth4webapi — already at their maximum, unmoved.",
    "",
    "OVERRIDES: sharp. Before this, `npm audit` on a freshly rendered surface",
    "reported 3 high-severity vulnerabilities: @builder.io/qwik-city -> vite-imagetools -> sharp",
    "<=0.35.4-rc.0, inheriting libvips CVE-2026-33327/33328/35590/35591 and libheif",
    "GHSA-g89c-p67h-r497 / GHSA-2jg2-4ch7-h545. `npm audit fix --force` would have",
    "DOWNGRADED qwik-city to 1.16.1 — a breaking change, away from the pinned line.",
    "sharp 0.35.4 is the first release outside the vulnerable range, so the override",
    "lifts the transitive floor and leaves qwik-city where the standard pins it.",
    "Measured after: `found 0 vulnerabilities`. Drop this override once qwik-city",
    "ships a vite-imagetools that depends on sharp >=0.35.4 itself."
  ],
  "name": "<project-name>-web-pwa",
  "description": "Qwik City installable PWA surface for <project-name> (mobile-pwa-first 2.0.0 candidate).",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18.11"
  },
  "scripts": {
    "dev": "vite --mode ssr",
    "build": "qwik build",
    "build.client": "vite build",
    "build.types": "tsc --incremental --noEmit",
    "preview": "qwik build preview && vite preview",
    "qwik": "qwik"
  },
  "dependencies": {
    "@builder.io/qwik": "^1.20.0",
    "@builder.io/qwik-city": "^1.20.0",
    "oauth4webapi": "^3.8.6"
  },
  "devDependencies": {
    "ignore": "7.0.9",
    "@types/node": "^24.13.4",
    "typescript": "^7.0.2",
    "vite": "=7.3.6",
    "vite-tsconfig-paths": "^6.1.1"
  },
  "overrides": {
    "sharp": "^0.35.4"
  }
}
