// <!-- Audit: B.4 (b4-mobile-only, FR-MO-010) -->
// Phase A skeleton — Phase B will wrap MaterialApp with BiometricLockWidget
// and provide AuthBloc / DeviceAttestor / SecureStorage via DI.

import 'package:flutter/material.dart';

class App extends StatelessWidget {
  const App({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: '{{project_name}}',
      theme: ThemeData(
        colorScheme: ColorScheme.fromSeed(seedColor: Colors.deepPurple),
        useMaterial3: true,
      ),
      home: const Scaffold(
        body: Center(
          child: Text('{{project_name}} — mobile-only scaffold'),
        ),
      ),
    );
  }
}
