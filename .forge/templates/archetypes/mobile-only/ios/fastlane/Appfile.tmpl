# <!-- Audit: B.4 (b4-mobile-only) -->
# iOS Fastlane Appfile — adopter MUST replace TEAM IDs.

app_identifier("{{reverse_domain}}")
apple_id(ENV["APPLE_ID"])
team_id(ENV["FASTLANE_TEAM_ID"])
itc_team_id(ENV["FASTLANE_ITC_TEAM_ID"])
