# <!-- Audit: B.1.14 (b1-delivery, FR-IN-009) -->
# <!-- Placeholder — replaced by Phase 4.4 GREEN. See tasks.md FR-IN-009. -->
# Frontend .env.dev — committed to repo. NEVER put secrets here.
# Secrets go in .env.local (gitignored).
# Provides OTLP HTTP defaults pointing at fsm-otel-collector:4318
# (Flutter SDK constraint — gRPC is unsupported in the official Dart
# OTel SDK at the time of this change).
