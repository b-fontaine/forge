# <!-- Audit: T.5 (t5-otel-stack, FR-OTEL-034) -->
# Sampler patch — dev env-tier ratio override.
# Standard: .forge/standards/observability.yaml v1.1.0 ::ratios.dev = 1.0
#
# Shipped explicitly (FR-OTEL-034 option a) for symmetry across the three
# overlays (dev / staging / prod), even though the value matches the base
# template default. Apply via configMapGenerator.behavior=merge in this
# overlay's kustomization, or via a strategic-merge patch when the OTel
# collector ConfigMap is wired in Phase B (see infra/CLAUDE.md "Sampler
# overlay mechanism" section).

processors:
  probabilistic_sampler:
    sampling_percentage: 100
