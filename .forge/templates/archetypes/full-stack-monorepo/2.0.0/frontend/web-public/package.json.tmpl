{
  "_audit": [
    "Audit: B.8.9 (b8-9-qwik-web-public, FR-B89-001/002/003/004/021)",
    "Standard: .forge/standards/web-frontend.yaml",
    "NEVER PUT SECRETS HERE — env values are supplied at deploy time, not committed.",
    "Pins verify-then-pin LIVE 2026-06-03 (evidence.md P-16..P-21). vite is pinned",
    "EXACTLY to 7.3.5: vite 8.x is the npm latest but is EXCLUDED by @builder.io/qwik",
    "peerDependencies '>=5 <8'. See web-frontend.yaml versions block + README PITFALL.",
    "Connect-ES runtime pins (^2.0.0) are owned by transport.yaml v1.3.0 (single",
    "source of truth). No zod dependency (deferred per ADR-B89-003)."
  ],
  "name": "<project-name>-web-public",
  "description": "Qwik City public-facing web surface for <project-name> (2.0.0 candidate; web-frontend.yaml).",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18.11"
  },
  "scripts": {
    "dev": "vite --mode ssr",
    "build": "qwik build",
    "build.client": "vite build",
    "build.types": "tsc --incremental --noEmit",
    "preview": "qwik build preview && vite preview",
    "qwik": "qwik"
  },
  "dependencies": {
    "@builder.io/qwik": "^1.20.0",
    "@builder.io/qwik-city": "^1.20.0",
    "@connectrpc/connect": "^2.0.0",
    "@connectrpc/connect-web": "^2.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.4.0",
    "vite": "=7.3.5",
    "vite-tsconfig-paths": "^4.2.1"
  }
}
