# Product Roadmap

This roadmap is a living document. Dates are approximate and may shift as
discovery reveals complexity. Items may move between phases. Ownership:
project maintainer reviews monthly; community input via GitHub Discussions
(Module D.6, opened 2026-04-30).

> **Source documents** : the audit roadmap
> (`/Users/bfontaine/.claude/plans/il-s-agit-l-d-un-noble-gem.md` — internal)
> establishes the original modular plan ; `docs/ARCHITECTURE-TARGET.md`
> (2026-04-29 rev v1.1) ratifies 10 ADRs that revise the archetype taxonomy
> (4 → 5) and the flagship's technical stack ; `docs/new-archetypes-plan.md`
> (2026-05-04) consolidates both into the post-v0.3.0 plan reflected below.

## Vision (6–12 Months)

By 2027-Q1, any Flutter or Rust team can bootstrap a Forge project in under
five minutes via an official installer, pick one of **five production-grade
archetypes** (`full-stack-monorepo`, `mobile-pwa-first`, `event-driven-eu`,
`ai-native-rag`, `rust-cli-tui`) and start a spec → implementation cycle the
same day. The framework is distributed under Apache License 2.0 and is
actively dog-fooded — Forge is developed *through* Forge, its own
`.forge/changes/` directory documenting every amendment to the Constitution
and every new capability. Public reference projects demonstrate each
archetype end-to-end. The deterministic linters run in CI on every
contributor's PR, blocking constitutional violations before review.
EU-friendly by design: the flagship stack defaults to Envoy Gateway,
DBOS-backed durable execution on Postgres, Connect-RPC, Zitadel identity,
and SigNoz + OBI eBPF + Coroot observability — with a graded compliance
profile (T1 RGPD-via-DPA, T2 self-hostable, T3 SecNumCloud / EUCS High).

## MVP (Now — 2026-Q2) — **Complete as of 2026-04-21 (v0.2.1)**

Corresponds to tier **T0** (immediate) and **T1** of the audit roadmap. All
MVP exit criteria are met. Residual T1 work (reference GitHub Actions
workflow, G.1) carried into Phase 2.

### MVP Items

| Status   | Item                                                                     | Audit ID | Shipped In |
|----------|--------------------------------------------------------------------------|----------|------------|
| Complete | Open-source license decision (Apache 2.0)                                | A1       | v0.1.0-t0  |
| Complete | Rewrite `LICENSE`, add `NOTICE` with upstream attribution                | A2       | v0.1.0-t0  |
| Complete | Fill `.forge/product/mission.md` with Forge's own mission                | E1       | v0.1.0-t0  |
| Complete | Fill `.forge/product/roadmap.md` with public roadmap                     | E2       | v0.1.0-t0  |
| Complete | Remove `defaultMode: plan` from project `.claude/settings.json`          | F7       | v0.1.0-t0  |
| Complete | Idempotent shell installer (`bin/forge-install.sh`)                      | A3       | v0.2.0     |
| Complete | `.forge/product/*` scaffolded from `.forge/templates/product/*`          | A3.0     | v0.2.0     |
| Complete | npm CLI package (`@sdd-forge/cli`) with `init`, `verify`, `version`      | A4       | v0.2.0     |
| Complete | CLI tarball bundles scaffoldable assets (fixed empty `init` from npm)    | A4.1     | v0.2.1     |
| Complete | Docker image `forge/linter:latest` bundling deterministic scripts        | A5       | v0.2.0     |
| Complete | SemVer policy coupled to the Constitution (`VERSION`, `docs/VERSIONING`) | A6       | v0.2.0     |
| Complete | `CODE_OF_CONDUCT.md`, `SECURITY.md`, `CHANGELOG.md`, issue/PR templates  | D1-D4    | v0.2.0     |

Change IDs remain `N/A` until `.forge/changes/` is populated retroactively
(audit item E3, scheduled for Phase 3). The `A4.1` sub-item was discovered
during T1 validation: `@sdd-forge/cli@0.2.0` shipped a three-file tarball
(`dist/`, `VERSION`, `README.md` only) that could not scaffold anything.
`v0.2.1` adds a `prepack` hook that bundles `.forge/`, `.claude/`, `bin/`,
`docs/`, and root artifacts into `cli/assets/` so `npx @sdd-forge/cli init`
produces a functional install.

### MVP Exit Criteria

- [x] Apache 2.0 `LICENSE` and `NOTICE` published at repository root.
- [x] `.forge/product/mission.md` and `.forge/product/roadmap.md` filled
  with real content (no HTML comment placeholders remaining).
- [x] `.claude/settings.json` no longer forces `defaultMode: plan` on all
  contributors.
- [x] `forge init` shell installer copies `.forge/`, `.claude/`, `.mcp.json`,
  `CLAUDE.md` into a target project idempotently.
- [x] `@sdd-forge/cli` npm package is installable and `forge init` scaffolds
  a functional Forge project from the published tarball (validated in
  `cli/test/e2e/cli.test.ts` > *published-tarball layout*).
- [x] `CODE_OF_CONDUCT.md`, `SECURITY.md`, `CHANGELOG.md`, GitHub issue and
  PR templates are present in the repository.

Note: the **first public reference project** (audit item C1) is deliberately
deferred to Phase 2, alongside the `full-stack-monorepo` archetype (B.1). An
example built before the archetype is defined would either diverge from the
canonical form or prematurely constrain archetype decisions. Building C1 via
`/forge:init --archetype full-stack-monorepo` — once that command exists —
makes the example free to produce and structurally coherent with what Forge
actually scaffolds.

## Phase 2 (Next — 2026-Q3)

Planned but not yet committed. Corresponds to audit tiers **T2** and the
start of **T3**.

### Phase 2 Items

| Priority | Capability                                                                                                                                                                                                 | Rationale                                                                                         |
|----------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|---------------------------------------------------------------------------------------------------|
| **Done** | **Archetype `full-stack-monorepo`** (Flutter + Rust + Infra) with CLAUDE.md scoping, protos as single source of truth via buf, multi-layer change workflow, and the `Janus` cross-layer orchestrator agent. **Delivered 2026-04-21 via `b1-foundations`** (schema, 3 standards, scoped commits, monorepo versioning, validator). **Delivered 2026-04-22 via `b1-scaffolder`** (archetype template tree, overlay renderer, `/forge:init --archetype` 7-step orchestrator, 3-level test harness, scaffold-manifest with tool versions). **Delivered 2026-04-23 via `b1-workflow`** (Janus agent + multi-root verify.sh / constitution-linter.sh + per-change `layers:` metadata + per-layer designs/tasks). **Delivered 2026-04-29 via `b1-delivery`** (4 reference GitHub Actions workflows with dorny/paths-filter, Kustomize base + 3 overlays dev/staging/prod with HPA, OTel + SigNoz local observability stack, 3 infra standards). **Schema promoted to `stable / 1.0.0` on 2026-04-29.** B.1 contract fully delivered ; 75/75 tests PASS across 4 harnesses. | Audit Module B.1 — flagship archetype, highest differentiation vs. alternatives                   |
| **Done** | **First public reference project** scaffolded via `/forge:init --archetype full-stack-monorepo`, with 4 demo changes (3 archived + 1 specified-only) demonstrating the full pipeline. **Delivered 2026-04-30 via `c1-reference-project`** : `examples/forge-fsm-example/` (~2.3 MB, 52 % of NFR-EX-002 5 MB budget) with 4 demo changes — `demo-001-greeting-service` (single-layer backend, gRPC Greeter + hexagonal Rust + cucumber-rs BDD), `demo-002-greeting-screen` (single-layer frontend, Flutter Cubit + bloc_test + widget tests), `demo-003-rate-limit` (multi-layer backend+infra triggering Janus orchestration with per-layer designs/tasks + Kong rate-limit plugin), `demo-004-user-onboarding` (multi-layer status:specified illustrating Article III.4 `[NEEDS CLARIFICATION:]` markers). Skip-guards in `verify.sh` + `constitution-linter.sh` (FR-GL-026/027), new `example` job in `forge-ci.yml` (FR-CI-012, paths-filter on `examples/**`). NFR-017 measured: **2124 bytes** overlay diff (52 % of budget). 30/30 tests PASS in `c1.test.sh`. New consolidated spec `.forge/specs/example-reference.md` for the `FR-EX-*` namespace. | Audit Module C.1 — coherent with the canonical archetype form (moved from T1 to avoid divergence) |
| **Done** | **`/forge:init` wizard with archetype auto-detection. Delivered 2026-04-30 via `b5-1-init-wizard`** : the npm CLI `forge init` is now the canonical entry point with three modes — `--archetype <name>` (explicit), `--auto` (signals heuristic on `pubspec.yaml` / `Cargo.toml`), `--wizard` (interactive Node `readline` prompt, zero new third-party deps). Dispatch table at `.forge/scaffolding/dispatch-table.yml` (2 active archetypes : `default` + `full-stack-monorepo` ; 3 placeholders : `flutter-firebase` / `mobile-only` / `rust-cli-tui` for B.2 / B.4 / B.3). Per-archetype wrappers under `bin/forge-init-<archetype>.sh` with stable ABI. Strict `[NEEDS DECISION:]` abort on auto ambiguity (Article III.4). NFR-IW-004 backwards-compat preserved (CI scripts invoking `forge init` without flags on non-TTY still work). New standard `global/scaffolding.md`, public `docs/ARCHETYPES.md` decision matrix, harness `b5.test.sh` 17/17 PASS, Vitest 56/56. New consolidated spec `.forge/specs/init-wizard.md`. | Audit Module B.5.1 — collapses onboarding friction ; dependency amont of B.2 / B.3 / B.4 |
| **Done** | **Reference GitHub Actions workflow running `verify.sh` + `constitution-linter.sh` + 4 test harnesses + CLI Vitest + shellcheck on every PR.** **Delivered 2026-04-29 via `g1-forge-ci`** : single-workflow `forge-ci.yml` with 5 jobs (harness/gates/cli/lint/summary), conditional `cancel-in-progress` PR/main asymmetry, minimal `contents: read` permissions, `cli/.nvmrc` Node 20.18.0 pinning, dedicated standard `global/forge-self-ci.md`, branch-protection guidance in `CONTRIBUTING.md`. Single required status `forge-ci / summary`. 14/14 tests PASS in `g1.test.sh`. | Audit Module G.1 — T1 carry-over, extends the blocking gates to CI                                |
| **Done** | **`forge upgrade` command: non-destructive merge of framework updates into a project. Delivered 2026-04-30 via `a7-forge-upgrade`** : new CLI subcommand `forge upgrade` (TS thin orchestrator + bash thick driver `bin/forge-upgrade.sh`), 3-way merge via `git merge-file --diff3` over paths declared in `.forge/framework-owned-paths.yml` (22 owned globs + 8 excluded), BASE recovery via committed snapshot tarballs (`full-stack-monorepo / 1.0.0` snapshot 422 KB gzipped, 41 % of 1 MB budget), conflict markers + `.merge-conflicts` companion, `--force` Git-cleanliness gate, major-version migration abort with `[NEEDS MIGRATION:]` marker, append-only `upgrade_history` ledger in `scaffold-manifest.yaml` with immutable identity fields, new standard `global/upgrade-policy.md` (6 H2 sections + 3 Interdictions), test harness `a7.test.sh` 29/29 PASS (L1 hermetic + L2 fixture truth-table + L3 opt-in against example tree), 5 Vitest unit tests for the TS layer. `examples/forge-fsm-example/` smoke test : 160 unchanged + 15 preserved + 0 conflicts, exit 0. New consolidated spec `.forge/specs/upgrade.md`. | Audit Module A.7 — adopters can now follow Constitution / standards / agents bumps without manual chores |
| Medium   | Pre-commit hook package (`forge-hooks`) for local constitution linting                                                                                                                                     | Audit Module G.2                                                                                  |
| Medium   | pipx packaging (`forge-cli`) as a Python-first alternative to `@sdd-forge/cli`                                                                                                                             | Audit Module A.4 complement — Python teams without Node on PATH                                   |
| Medium   | Homebrew formula (macOS dev ergonomics)                                                                                                                                                                    | Audit Module A.8                                                                                  |
| **Done** | **Persistent `[NEEDS CLARIFICATION]` tracking per change (`open-questions.md`). Delivered 2026-05-01 via `f1-open-questions`** : new standard `global/open-questions.md` (8 H2 sections + 3 Interdictions), template `open-questions.md.tmpl`, per-change file convention with Q-NNN sequential / Status enum (open/answered/wontfix) / Resolution block, **`verify.sh` Open Questions Gate** blocks archive on lingering `Status: open` questions, **`constitution-linter.sh` Article III.4 rule** blocks `[NEEDS CLARIFICATION:` inline in `implemented` or `archived` changes (with smart exclusions for backticks / HTML comments / fenced code blocks), **`bin/forge-questions.sh`** discovery script with `--change` / `--status` filters. Backwards-compatible : 10 pre-F.1 archived changes (b1-*, g1, c1, a7, b5-1, d5, b4) NOT backfilled, gate skips on absent file. 17/17 tests PASS in `f1.test.sh` (12 L1 + 5 L2 fixture-based). 11 harnesses now run in CI. New consolidated spec `.forge/specs/open-questions.md`. Documentation: `docs/OPEN_QUESTIONS.md`. | Audit Module F.1 — robustness; F.2 + F.4 still pending in T3 |
| **Done** | **JSON Schema validating `.forge.yaml` per change, enforced by `verify.sh`. Delivered 2026-05-01 via `f2-yaml-schema`** : new `.forge/schemas/change.schema.json` (Draft 2020-12) with required name/status/created/schema/constitution_version, status enum (6 values), schema enum dynamique with drift detector test, semver pattern, timeline shape + coherence rules. New `.forge/scripts/validate-change-yaml.sh` (bash + Python 3 inline, no jsonschema lib). New `verify.sh` section "Change YAML Schema" iterates over `.forge/changes/*/.forge.yaml`. New standard `global/change-yaml-schema.md`, doc `docs/SCHEMA.md`, harness `f2.test.sh` 18/18 PASS. **All 11 archived pre-F.2 changes validated** (NFR-YS-001 backward compat) — schema accommodates historical extended fields (`parent_audit_items`, `depends_on`, `archived_to`, `schema_promotion`, `promotes_schema`). PyYAML date coercion implemented (unquoted ISO dates parsed as `datetime.date` are converted to strings before pattern check). 12 harnesses now run in CI. New consolidated spec `.forge/specs/change-yaml-schema.md`. | Audit Module F.2 — robustness; F.4 still pending in T3 |
| **Done** | **`constitution-linter.sh` extended to cover Articles V, X.3, XI.3, XI.5 (previously not enforced). Delivered 2026-05-01 via `f4-linter-extension`** : 4 new sections in the linter — Article V.1 (task ↔ FR linkage in tasks.md), Article X.3 (public API doc ratio ≥ 80% Dart/Rust with first-5-missing list), Article XI.3 (GenUI schema-driven heuristic warning, NOT fail), Article XI.5 (fallback test pair `*fallback*` ↔ `*fallback*_test*`). Opt-out env vars per rule (`FORGE_LINTER_SKIP_V_1`, `_X_3`, `_XI_3`, `_XI_5`) + threshold override (`FORGE_LINTER_X3_THRESHOLD`). New `warn` helper + WARN counter. New standard `global/linting-rules.md` (6 H2 + opt-out matrix), doc `docs/LINTING.md` (~140 lignes). Harness 23/23 PASS (16 L1 + 7 L2 fixture-based). 13 harnesses now run in CI. Linter perf 1.97s ≤ 3s budget. Constitution coverage estimated ~70% → ~85%. **T3 robustness 100% delivered** (F.1 + F.2 + F.4). | Audit Module F.4 — DONE; T3 robustness complete |
| **Done** | **Governance model (`GOVERNANCE.md`) — amendment process, release ownership, BDFL vs. committee decision. Delivered 2026-04-30 via `d5-governance`** : `GOVERNANCE.md` at repo root (BDFL-with-fallback model — current phase Benoit Fontaine `@bfontaine` ; mature phase committee 3-7 members behind future amendment), `CODE_OF_CONDUCT.md` at repo root (Contributor Covenant v2.1 verbatim, contact `contact@benoitfontaine.fr`), Constitution amended v1.0.0 → v1.1.0 (new **Article XII — Governance** delegating operational rules to `GOVERNANCE.md`, first row in `## Amendments` table), templates bumped (`change.yaml` × 2 + archetype `.forge.yaml.tmpl`), README `## Governance` section now links both files. ADR-006 establishes the canonical precedent : a change-amendment ratified UNDER version N stays at N in its `.forge.yaml` and CREATES N+1 (no circular reference). 15/15 tests PASS in `d5.test.sh` ; 9 harnesses now run in CI (`b5.test.sh` + `d5.test.sh` newly registered). New consolidated spec `.forge/specs/governance.md`. | Audit Module D.5 — closes T2 P1 (last facilitator) and unblocks the second-archetype work in T3 P2 |
| **Done** | **Archetype `mobile-only` (Flutter iOS + Android, OIDC via `flutter_appauth`, no BaaS) — second archetype premium. Delivered 2026-04-30 via `b4-mobile-only` in 3 phases**: Phase A (core scaffold structure, schema + wrapper bash + dispatch entry + Flutter/iOS/Android skeleton + snapshot 436 KB), Phase B (runtime modules: oidc_config + auth_repository + auth_bloc + secure_storage_adapter + biometric_service + biometric_lock_widget + DeviceAttestor + 3 impls iOS App Attest / Android Play Integrity / Fake + Swift bridge AppAttestService + Kotlin bridge PlayIntegrityService + OTel init + flutter-mobile.md standard 7 H2 + 3 Interdictions), Phase C (Fastlane per-platform with secrets via ENV exclusively + mobile-ci.yml.tmpl iOS macos-latest + Android ubuntu-latest + e2e opt-in + ARCHETYPES.md row + framework-owned-paths.yml.tmpl per-archetype). 47/47 tests PASS in `b4.test.sh` (42 L1 + 5 L2). 10 harnesses now run in CI ; total ≥ 234 tests on `optim`. Snapshot 465 KB gzipped (23 % of NFR-MO-001 budget 2 MB). New consolidated spec `.forge/specs/mobile-only.md`. **First validation of B.5.1 dispatcher ABI**: zero TypeScript edit, adding mobile-only = 1 dispatch-table.yml entry + 1 `bin/forge-init-mobile-only.sh` wrapper. **First change ratified under Constitution v1.1.0** (post-D.5). **Note 2026-05-04**: schema `mobile-only / 1.0.0` becomes a legacy compat alias for the renamed `mobile-pwa-first` archetype (see Phase 3 below). | Audit Module B.4 — closes T2 P2 (second-archetype gate) ; **guard-rail PR optim → main + v0.3.x release now liftable at user discretion** |

### v0.3.0 Release (2026-05-02)

PR #1 merged (`47f8232`), tag `v0.3.0` (`dc0e1ce`) pushed, npm
`@sdd-forge/cli@0.3.0` published. **13 changes archived** on `optim`,
**292/292 tests** PASS across **13 harnesses**, **`verify.sh`** 108 PASS / 0 FAIL,
**`constitution-linter.sh`** OVERALL PASS, Constitution **v1.1.0**. Three
rounds of CI fix-ups required (`c29c9ce` + `f4626e6` + `a22d8c0` + `5cea6c3`)
because `forge-ci.yml` only ran on PR-to-main, never on push-to-optim. GH
release pending manual creation (gh CLI absent on maintainer's machine).
Bug detected in `scripts/release-v0.3.0.sh` (cumulative `cd` via `eval`,
2FA OTP not handled) — **fixed 2026-05-12 via `f3-release-script-fix`**
(F.3 pulled forward from T8 ; the script is renamed `scripts/release.sh`
with `--version` + `--otp` flags ; see entry below).

### v0.3.x deliveries since v0.3.0 (2026-05-04 → 2026-05-05)

| Status         | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Plan ID            | Shipped In                                              |
|----------------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|--------------------|---------------------------------------------------------|
| **Done**       | **Methodology ratification — T4 in one shot.** All five T4 methodology pre-requisites of `docs/new-archetypes-plan.md` §2 (P-1 through P-4) **delivered as a single consolidated change** rather than ten separate ADR archives — the audit trail is preserved through 35 ADDED FRs (`FR-T4-ADR-001..010` + `FR-T4-STD-001..006` + `FR-T4-LC-001..005` + `FR-T4-SCH-001..002` + `FR-T4-LNT-001..002` + `FR-T4-TST-001..005` + `FR-T4-IDX-001..002` + `FR-T4-DSP-001` + `FR-T4-DOC-001..003`) + 8 NFRs in `.forge/specs/adr-ratification.md`. Six versioned `.forge/standards/*.yaml` shipped at v1.0.0 (`transport`, `state-management`, `observability`, `orchestration`, `identity`, `persistence`). 12-month review cycle wired (`global/standards-lifecycle.md` + `REVIEW.md` ledger). Two new JSON schemas (`compliance-tier.schema.json` T1/T2/T3 + `archetype.schema.json` v2 with the 5-archetype enum + `mobile-only` legacy alias). Constitution v1.1.0 unchanged ; ADR-006 amendment-versioning precedent reused. **P-5 (Hera 9 → 5 sub-agents) retired 2026-05-06** — rejected by maintainer as an unsourced architect opinion ; the 9 sub-agents stay. | P-1, P-2, P-3, P-4, I.1, J.1–J.6 of `new-archetypes-plan.md` | Archived 2026-05-04 via `t4-adr-ratification` (PR #2)   |
| **Done**       | **Connect codegen on the flagship template (T5 Phase 1, additive). Delivered 2026-05-06 via `t5-connect-codegen`** (PR #3, merge commit `ca27257`). Adds `protoc-gen-connect-go` v1.19.2 + `bufbuild/protoc-gen-es ≥ v2.2.0` (Connect v2) + `connectrpc/connect-dart` v1.0.0+ official to `templates/full-stack-monorepo/1.0.0/proto/buf.gen.yaml.tmpl` ; bumps `transport.yaml` 1.0.0 → 1.1.0 with codegen pinning (11 versions) + REVIEW.md `Updated` entry ; pivots to **`connectrpc-build` via `build.rs`** (Option 2 / Path α) after T-BUF investigation revealed the BSR remote plugin is not yet shipped — the codebase convention is "remote plugins only", so `connectrpc-build` is consumed as a build-dependency in `backend/crates/grpc-api/Cargo.toml.tmpl` instead. Pins `connectrpc = "=0.3.3"` + `buffa = "=0.3.3"` + `buffa-types = "=0.3.3"` (Anthropic OSS, Apache-2.0, 6 558 conformance tests, MSRV 1.88). Adds parallel axum Connect route via `Router::into_axum_service()` ; tonic-build untouched (ADR-004 KEEP). New demo `demo-005-connect-greeting` archived in `examples/forge-fsm-example/`. New WARN-only linter rule `transport-codegen-coverage` (opt-out `FORGE_LINTER_SKIP_TRANSPORT_CODEGEN=1`). Test harness `t5.test.sh` 25/25 L1 PASS ; **L2 fixtures (T-L2-001..007) deferred to T6** (B.8 flagship migration). New consolidated spec `.forge/specs/connect-codegen.md` (32 FRs `FR-T5-CC-001..072` + 10 NFRs + 6 ADRs `ADR-T5-001..006`). | T5 / Phase 1 ARCH item #1 | Archived 2026-05-06 via `t5-connect-codegen` (PR #3)   |
| **Done**       | **Standards YAML automated enforcement (J.7). Delivered 2026-05-08 via `j7-validate-standards-yaml`.** New deterministic linter `bin/validate-standards-yaml.sh` (bash thin + Python 3 inline, F.2 pattern lifted, no `jsonschema` lib) + JSON Schema Draft 2020-12 at `.forge/schemas/standard.schema.json` enforcing the 8-field frontmatter contract from `t4-adr-ratification` (FR-J7-001..010). Five blocking lifecycle invariants : Article XII coupling `expires_at: never` ⇔ `exception_constitutional: true` (FR-J7-020), strict `expires_at > last_reviewed` (FR-J7-021), REVIEW.md full ledger scan drift (FR-J7-023, ADR-J7-003), `linter_rule` cross-reference into `constitution-linter.sh` via `(echo|#)` section anchor grep (FR-J7-030, ADR-J7-002), `index.yml` trigger reachability (FR-J7-050). Two non-blocking informational rules (12-month cycle loose FR-J7-022, orphan standards FR-J7-051). New `verify.sh` section "Standards YAML Schema" after F.2 (+7 PASS on the live tree, 6 standards + 1 trigger reachability). Harness `j7.test.sh` 21/21 GREEN at `--level 1,2`, live-tree perf 122 ms (NFR-J7-001 budget 2 s, 6 % used). New consolidated spec `.forge/specs/standards-yaml-validation.md` (27 FRs + 6 NFRs + 5 ADRs). Documentation : `docs/SCHEMA.md` § "Standard YAML schema" + `global/standards-lifecycle.md` § "Automated enforcement" + CHANGELOG entry. | T5 / J.7 ARCH §12.1 | Archived 2026-05-08 via `j7-validate-standards-yaml`   |
| **Done**       | **OTel + OBI + Coroot stack templates (T.5 Phase 1, infra side, additive). Delivered 2026-05-10 via `t5-otel-stack`.** Realises `observability.yaml` v1.1.0 (ADR-008) on the `full-stack-monorepo / 1.0.0` archetype : OBI eBPF DaemonSet `grafana/beyla:2.0.1` with **unprivileged-with-capabilities** posture (ADR-OTEL-004 — caps `BPF/SYS_PTRACE/NET_RAW/CHECKPOINT_RESTORE/DAC_READ_SEARCH/PERFMON/NET_ADMIN/SYS_ADMIN`, `drop ALL`, dedicated RBAC, `forge.dev/aegis-audit: required` annotation, `forge.dev/kernel-min-58: "true"` nodeSelector) + Coroot Deployment `coroot/coroot:1.4.4` multi-doc YAML (ADR-OTEL-006) + `processors.probabilistic_sampler` block in OTel collector (ADR-OTEL-001 — `mode: proportional`, `attribute_source: traceID`, `hash_seed: 22`) wired into traces pipeline only + 3 env-tier overlay sampler-patches (`infra/k8s/overlays/{dev,staging,prod}/sampler-patch.yaml.tmpl` with ratios 100/100/10 per `observability.yaml::ratios`). Standard bumped 1.0.0 → 1.1.0 with `versions:` block recording `beyla: "2.0.1"` + `coroot: "1.4.4"` (ADR-OTEL-003, symmetric with T.5 `transport.yaml` 1.0.0 → 1.1.0). REVIEW.md ledger gains `Updated` entry 2026-05-09. `infra/CLAUDE.md.tmpl` gains 3 H2 sections (Privileged DaemonSet — Aegis duty / Sampler overlay mechanism / Coroot persistence) ; `infra/k8s/base/README.md.tmpl` gains "Deployment prerequisites" checklist. Snapshot tarball regenerated 520 KB (87 % of NFR-OTEL-001 budget 600 KB), `a7.test.sh` 29/0 PASS (forge upgrade backward compat). Harness `t5-otel.test.sh` 14/14 GREEN at `--level 1`, registered in `forge-ci.yml` matrix. Example mirror parity in `examples/forge-fsm-example/infra/`. New consolidated spec `.forge/specs/otel-stack.md` (22 FRs `FR-OTEL-001..082` + 5 NFRs + 7 ADRs `ADR-OTEL-001..007`). | T5 / OTel infra ARCH §12.1 + ADR-008 | Archived 2026-05-10 via `t5-otel-stack`   |
| **Done**       | **Forge compliance reusable workflow (I.5). Delivered 2026-05-12 via `i5-compliance-workflow`.** New `.github/workflows/forge-compliance.yml` (158 LOC, `on: workflow_call:` trigger with 3 inputs `eu-tier` (required) / `target-dir` (default `.`) / `artefact-name` (default `forge-compliance-artefacts`) + 1 output `artefact-path` ; `permissions: contents: read` ; concurrency keyed on ref + tier with `cancel-in-progress: false`). Orchestrates the four EU-compliance scripts shipped by I.3 + I.6 + K.3 + J.8.d : Demeter (`bin/forge-demeter-scan.sh`) + constitution linter incl. ADR-I3-001 T3-Forbidden section (`.forge/scripts/constitution-linter.sh`) + CycloneDX SBOM (`bin/forge-sbom.sh`) + bundle generator (`.forge/scripts/compliance/bundle.sh`). Uploads the deterministic `.tgz` via `actions/upload-artifact@v4`. Six physical deliverables : **(I.5.a)** the workflow YAML itself ; **(I.5.b)** `.forge/standards/global/forge-compliance-workflow.md` v1.0.0 (284 LOC, 7 H2 sections — Purpose & EU compliance rationale / Workflow inputs and outputs / Step-by-step contract / Tier-scaled severity aggregation / Consumption protocol / Forward compatibility / Interdictions + Constitutional Compliance — 4 RFC-2119 MUST NOT clauses, frontmatter pins `version: 1.0.0`, `last_reviewed: 2026-05-12`, `expires_at: 2027-05-12`, `linter_rule: null` since the workflow itself is the enforcement surface) ; **(I.5.c)** `.forge/standards/index.yml` entry registered under "I.5 — Forge compliance workflow" section (id `global/forge-compliance-workflow`, 8 triggers `compliance, forge-compliance.yml, reusable-workflow, workflow_call, eu-tier, ci-enforcement, regulatory-handoff, github-actions`, scope `all`, priority `high`) ; **(I.5.d)** `.forge/standards/REVIEW.md` append-only birth entry 2026-05-12 (KEEP, next review 2027-05-12) ; **(I.5.e)** `docs/COMPLIANCE.md` new H2 "Reusable compliance workflow" with copy-pasteable `uses:` YAML block, tier-inheritance notes, and chained-output guidance ; **(I.5.f)** `.forge/scripts/tests/i5.test.sh` 16 L1 grep-based tests + 1 L2 opt-in fixture (`FORGE_I5_ACT=1` + `command -v act` gates per ADR-I5-CW-003, skip-when-absent semantics mirror `t5-otel-live-run::FORGE_LIVE_RUN_DOCKER=1`) registered in `forge-ci.yml` matrix after `i3.test.sh` with `--level 1`. Three ADRs (ADR-I5-CW-001..003) resolve exit-code aggregation (trust each script's tier scaling end-to-end ; SBOM no-lockfile non-fatal via `continue-on-error: true`), `SOURCE_DATE_EPOCH` source (commit timestamp with run-start fallback ; no `inputs.epoch`), and L2 act-runner gating. Harness `i5.test.sh` **17/17 GREEN at `--level 1,2`** (16 L1 + 1 L2 skip-pass) ; `verify.sh` overall PASS preserved (no regression) ; `constitution-linter.sh` OVERALL PASS preserved ; `forge-ci.yml` ≤ 300 lines (NFR-CI-002, 281 lines) ; workflow YAML ≤ 200 lines (NFR-I5-CW-002, 158 lines). New consolidated spec `.forge/changes/i5-compliance-workflow/specs.md` (`FR-I5-CW-001..142` + 9 NFRs + 3 ADRs `ADR-I5-CW-001..003`). Resolves the `compliance-artefacts-bundle.md::Consumption protocol` forward-pointer ("the I.5 future workflow has not shipped yet"). Forward-stable for Themis-territory regulatory deadline steps (NIS2 / DORA / CRA / AI Act) when K.5 (T7+) ships — additive step additions per FR-I5-CW-083. | T5 / I.5 ARCH §10 + ADR-007/008/010 | Archived 2026-05-12 via `i5-compliance-workflow`   |
| **Done**       | **Compliance artefacts bundle (I.6). Delivered 2026-05-12 via `i6-compliance-artefacts`.** Deterministic `.tgz` regulatory hand-off bundle generator + DPA template + standard, packing every compliance-relevant Forge artefact into a single byte-stable archive auditors and regulator counter-parties can consume directly. Seven physical deliverables : **(I.6.a)** `.forge/scripts/compliance/bundle.sh` — bash thin + Python 3 inline (mirrors `bin/forge-sbom.sh` per NFR-I6-CA-004, F.2/J.7/J.8.d pattern reused) ; six bundle members emitted in lexicographic order (`MANIFEST` + `tier-matrix/compliance-tiers.md` + `templates/forge-dpa-declared.template` + `audit/audit-ledger.json` + `audit/audit-ledger.md` + `sbom/sbom.cdx.json`) ; tarball assembly via Python stdlib `tarfile` + `gzip.GzipFile` two-step idiom pinning per-member `mtime`/`uid`/`gid`/`mode` + gzip header `mtime` for `SOURCE_DATE_EPOCH` byte-identity (ADR-I6-CA-001) ; `bin/forge-sbom.sh` invoked for the SBOM member with `--target` propagation + non-fatal handling of "no lockfiles" (FR-I6-CA-019, minimal CycloneDX 1.5 envelope fallback) ; exit codes `0` success / `1` missing source artefact / `2` usage error. **(I.6.b)** `.forge/templates/compliance/forge-dpa-declared.template` — canonical DPA declaration template mirroring K.3 ADR-K3-002 ledger format, cross-references K3-RULE-002 (T1 + ⚠️-T1 without DPA → High finding) + K3-RULE-002a staleness window (13 months + 1 month grace per RGPD Article 5(1)(e)), canonical example line `T1: 2026-04-15 LegalOps-Confluence-DPA-2026-Q2`. **(I.6.c)** `.forge/standards/global/compliance-artefacts-bundle.md` v1.0.0 — 6 H2 sections (Purpose & EU compliance rationale / Bundle content schema / Determinism guarantee / Consumption protocol / Regeneration cadence / Interdictions) + Forward compatibility + Constitutional Compliance ; 6-row member table ; 4 RFC-2119 MUST NOT clauses (no PII / secrets ; no `--no-deterministic` escape hatch ; no Themis-territory artefacts before K.5 ; no post-emission bundle modification) ; frontmatter pins `version: 1.0.0`, `last_reviewed: 2026-05-12`, `expires_at: 2027-05-12`, `linter_rule: null` (advisory standard, no constitution-linter section). **(I.6.d)** `.forge/standards/index.yml` entry registered under new "I.6 — Compliance artefacts bundle" section (id `global/compliance-artefacts-bundle`, 10 triggers `compliance, bundle, auditor, dpa, audit-ledger, nis2, dora, cra, ai-act, regulatory-handoff`, scope `all`, priority `high`). **(I.6.e)** `.forge/standards/REVIEW.md` append-only birth entry dated 2026-05-12 (KEEP, next review 2027-05-12). **(I.6.f)** `docs/COMPLIANCE.md` new H2 `## Auditor hand-off bundle` cross-linking the bundle script + the standard + the determinism recipe (Quick start + Bundle members + Determinism with `SOURCE_DATE_EPOCH`). **(I.6.g)** `.forge/scripts/tests/i6.test.sh` — 14 L1 hermetic grep-based tests (script presence + executable / `--help` exit 0 / audit comment / bogus arg exit 2 / template presence + canonical example / standard presence + frontmatter + 6 H2 + ≥ 3 MUST NOT / index entry / REVIEW.md entry / docs/COMPLIANCE.md H2 / CHANGELOG entry) + 2 L2 fixture tests (good-fixture bundle producing 6 members + sorted MANIFEST ; determinism via `SOURCE_DATE_EPOCH=0` × 2 invocations + `diff -q` byte-identity assertion). Registered in `forge-ci.yml` `harness` matrix after `i2.test.sh` ; CI file stays under NFR-CI-002 300-line budget (271 lines). Harness `i6.test.sh` **16/16 GREEN at `--level 1,2`** ; verify.sh + constitution-linter.sh OVERALL PASS preserved. Three ADRs (`ADR-I6-CA-001..003`) resolve the design open questions : `.tgz` gzip POSIX tar archive format (Reproducible-Builds recipe) ; `audit/` subdirectory layout for the ledger snapshot ; script location under `.forge/scripts/compliance/` (framework-internal, not adopter-facing CLI). Bundle layout is **forward-stable** for Themis-territory artefacts (NIS2 / DORA / CRA / AI Act regulatory deadlines under `.forge/compliance/{nis2,dora,cra,ai-act}/`) — additive expansion under a new `regulatory/` subdirectory + a SemVer minor bump per FR-I6-CA-053 when Themis (K.5, T7+) ships, no breaking schema change. Scope deliberately **narrower** than the original I.6 plan : the bundle generator + content schema + deterministic emission ship now ; the NIS2/DORA/CRA/AI Act regulatory-deadline artefacts themselves stay in Themis territory. New consolidated spec `.forge/changes/i6-compliance-artefacts/specs.md` (45 FRs `FR-I6-CA-001..115` + 9 NFRs `NFR-I6-CA-001..009` + 3 ADRs). | T5 / I.6 ARCH §10 + ADR-007/008/010 | Archived 2026-05-12 via `i6-compliance-artefacts`   |
| **Done**       | **T3-Forbidden Components linter rule (I.3). Delivered 2026-05-12 via `i3-t3-forbidden-linter`.** New `.forge/scripts/constitution-linter.sh` ADR-I3-001 section enforcing the `forbidden:` frontmatter contract across every `.forge/standards/*.yaml` manifest (T.4 contract) + body + ADR text. Tier-scaled severity per ADR-I3-002 / Q-001 resolution : T1 + T2 `WARN` Phase A → `FAIL` at B.8 / T6 via SemVer minor bump of the new standard (1.0.0 → 1.1.0) ; **T3 `FAIL` immediate** (mirrors J.8 `--eu-tier T3` refusal pattern — T3 declares 100 % EU jurisdiction so a forbidden component is by definition unacceptable). Five physical deliverables : **(I.3.a)** `.forge/standards/global/forbidden-components-rules.md` v1.0.0 (240 LOC, 10 seed rules `T3-RULE-001..010` inheriting ADR-J8-004 namespace ; sibling rule-catalogue surface to J.8 `J8-RULE-*` and K.3 `K3-RULE-*`) ; **(I.3.b)** `.forge/standards/index.yml` entry registered under "I.3 — Forbidden components rules" (id `global/forbidden-components-rules`, 10 triggers `forbidden, t3-forbidden, t3-rule, linter, t1, t2, t3, eu-tier, ci-enforcement, forbidden-components`, scope `all`, priority `high`) ; **(I.3.c)** `.forge/standards/REVIEW.md` append-only birth entry 2026-05-12 (KEEP, next review 2027-05-12) ; **(I.3.d)** `docs/LINTING.md` new H2 "ADR-I3-001 — T3-Forbidden Components" with severity matrix + opt-out env var `FORGE_LINTER_SKIP_T3_FORBIDDEN` ; **(I.3.e)** `.forge/scripts/tests/i3.test.sh` 14 L1 + 4 L2 fixture-based tests registered in `forge-ci.yml` matrix after `i2.test.sh`. `.forge/standards/global/compliance-tiers.md` flips its `linter_rule:` forward-pointer from documentation-only to executable. Harness `i3.test.sh` **14/14 GREEN at `--level 1`** ; verify.sh 191 PASS / 0 FAIL / 1 WARN ; constitution-linter OVERALL PASS (29 sections, +1 new). New consolidated spec under `.forge/changes/i3-t3-forbidden-linter/specs.md` (`FR-I3-T3F-001..126` + 9 NFRs + 4 ADRs `ADR-I3-001..004`). Resolves I.2's `compliance-tiers.md::linter_rule: t3-forbidden-components` forward-pointer. **Unblocks I.5** (`forge-compliance.yml` reusable workflow consumes this rule's exit code envelope). | T5 / I.3 ARCH §10 + ADR-007/008/010 | Archived 2026-05-12 via `i3-t3-forbidden-linter`   |
| **Done**       | **EU compliance tiers standard T1 / T2 / T3 (I.2). Delivered 2026-05-12 via `i2-compliance-tiers`.** Single human-readable standard codifying the EU compliance gradient T1 / T2 / T3 from `compliance-tier.schema.json` v1.0.0 (T.4) and `ARCHITECTURE-TARGET.md` §10. Five physical deliverables : **(I.2.a)** `.forge/standards/global/compliance-tiers.md` v1.0.0 (437 LOC, 7 H2 sections — Purpose / Tier definitions / Component eligibility matrix / Demeter integration / Adoption path / Extending the matrix / Interdictions + Constitutional Compliance) ; verbatim citation of the schema's `x-tier-descriptions` block per Article III.4 + K.3 FR-K3-DEM-020 precedent (NFR-I2-CT-004) ; 15-row component matrix byte-identical to `ARCHITECTURE-TARGET.md` §10.2 (ADR-I2-CT-002 / NFR-I2-CT-005) ; 5 RFC-2119 MUST NOT clauses ; frontmatter pins `linter_rule: t3-forbidden-components` as a kebab-case forward-pointer to I.3 (ADR-I2-CT-001) ; **(I.2.b)** `.forge/standards/index.yml` entry registered under "I.2 — Compliance tiers" section (id `global/compliance-tiers`, 9 triggers `compliance, t1, t2, t3, eu-tier, dpa, schrems, cloud-act, tier-classification`, scope `all`, priority `high`) ; **(I.2.c)** `.forge/standards/REVIEW.md` append-only birth entry dated 2026-05-11 (KEEP, next review 2027-05-11) ; **(I.2.d)** `docs/COMPLIANCE.md` adopter-facing intro at repository root per ADR-I2-CT-003 (H1 + 3 H2 sections — Quick start / Tier picker / Cross-references + decision tree) ; **(I.2.e)** `.forge/scripts/tests/i2.test.sh` harness 14 L1 hermetic grep-based tests registered in `forge-ci.yml` matrix after `k3.test.sh`. Resolves the Demeter forward-pointer shipped by K.3 on 2026-05-12 ; unblocks I.3 (T3-forbidden linter rule), I.5 (`forge-compliance.yml` workflow), I.6 (regulatory artefacts NIS2 / DORA / CRA / AI Act). Harness `i2.test.sh` **14/14 GREEN at `--level 1`** ; verify.sh 165 PASS / 0 FAIL / 1 WARN ; constitution-linter OVERALL PASS ; validate-standards-yaml STD-PASS preserved (MD standard, out of J.7 scope). New consolidated spec `.forge/specs/compliance-tiers.md` (FR-I2-CT-001..112 + 7 NFRs + 3 ADRs `ADR-I2-CT-001..003`). | T5 / I.2 ARCH §10 + ADR-007/008/010 | Archived 2026-05-12 via `i2-compliance-tiers`   |
| **Done**       | **Demeter data-steward agent + CLOUD Act dependency scanner (K.3 + I.4). Delivered 2026-05-12 via `k3-demeter`.** Six physical deliverables under one umbrella change : **(K.3.a)** `.claude/agents/demeter.md` persona file (7 H2 sections — Persona, Purpose, Checklists, Output, Rule Catalogue, Integration, Anti-Hallucination Protocol ; Aegis stylistic pattern, severity-first/evidence-driven) ; **(K.3.b)** `bin/forge-demeter-scan.sh` deterministic dependency scanner (F.2 / J.7 / J.8.d pattern verbatim — bash thin + Python 3 inline, zero new external dep) walking Cargo / npm / pubspec lockfiles under `--target` recursively (depth ≤ 3) and matching publishers against the curated deny-list ; tier-scaled severity per FR-K3-DEM-068 (T1 → Informational ; T2 → High ; T3 → Critical) ; exit code envelope 0/1/2/3 mirrors J.8 ADR-J8-003 policy-refusal semantics ; `SOURCE_DATE_EPOCH`-deterministic byte-identical output per NFR-K3-DEM-005 ; **(K.3.c)** `.forge/data/cloud-act-publishers.yml` deny-list seeding 4 cargo + 4 npm + 2 pub publisher patterns covering AWS / Google Cloud / Azure / Firebase ecosystems with citable evidence URLs ; **(K.3.d)** `.forge/standards/global/data-stewardship-rules.md` authoritative standard (7 H2 sections — rule catalogue, adoption path, DPA declaration semantics, extending the catalogue, regeneration cadence Phase A interim BDFL / 12-month vs Phase B post-T7 Themis / 6-month per ADR-K3-003, constitutional compliance) ; standards index registers entry under "Cross-Cutting Standards" ; **(K.3.e)** `.forge/scripts/tests/k3.test.sh` harness 20 L1 + 2 L2 tests, registered in `forge-ci.yml` matrix after `j8.test.sh` ; **(K.3.f)** Janus delta integration (`cross-layer-orchestrator.md` Step 9 rename `Security Pass (Aegis)` → `Security & Data-Stewardship Pass (Aegis + Demeter)` + new dispatch row + Quality Gates bullet per ADR-K3-007). DPA ledger surface = `.forge/.forge-dpa-declared` plain-text 1-line (mirrors J.8 ADR-J8-006 `.forge-tier` ledger verbatim ; ADR-K3-002 resolves Q-001). Rule-ID namespace = `K3-RULE-NNN` (inherits ADR-J8-004 format) with 5 seed rules (K3-RULE-001 US-jurisdiction publisher / K3-RULE-002 DPA undeclared at T1 / K3-RULE-003 tier downgrade refused / K3-RULE-004 data classification missing / K3-RULE-005 Cargo workspace drift) + K3-RULE-006 operational guardrail (publisher list staleness). Repository-level `CLAUDE.md` agent table gains Demeter trigger row alphabetically between Oracle and Clio. Harness `k3.test.sh` **22/22 GREEN at `--level 1,2`** ; verify.sh 158 PASS / 0 FAIL / 1 WARN ; constitution-linter OVERALL PASS. New consolidated spec `.forge/specs/data-stewardship.md` (55 FRs `FR-K3-DEM-001..124` + 9 NFRs + 7 ADRs `ADR-K3-001..007`). | T5 / K.3 + I.4 ARCH §9.2 + §10 | Archived 2026-05-12 via `k3-demeter`   |
| **Done**       | **Janus orchestrator rules + EU compliance tier + CycloneDX SBOM (J.8). Delivered 2026-05-10 via `j8-janus-rules`.** Three sub-modules : **(J.8.a)** Janus refusal rules — `cross-layer-orchestrator.md` gains "Forbidden archetypes & combinations" H2 section enumerating `J8-RULE-001` (flutter-firebase Schrems II + CLOUD Act), `J8-RULE-002` (T3 ⇒ self-host Zitadel), `J8-RULE-003` (T3 ⇒ self-host SigNoz + no Datadog) ; `dispatch-table.yml` gains `forbidden_archetypes:` runtime registry ; shared bash helper `bin/_forge-init-helpers.sh::_refuse_if_forbidden()` (Python 3 inline reads dispatch-table, emits `[REFUSAL: <archetype>: <rule_id>: <reason> ; alternative: <alt>]` stderr + exit 3) sourced by both `forge-init-fsm.sh` + `forge-init-mobile-only.sh` ; TS dispatcher (`init-archetype.ts`) is first line of defense with new `ForbiddenArchetypeEntry` interface ; new standard `global/janus-orchestration-rules.md` (≥ 5 H2 sections). **(J.8.b)** `--eu-tier T1\|T2\|T3` flag plumbing — `init.ts` gains `EU_TIER_ENUM` typed const + optional `euTier?:` field validated against `compliance-tier.schema.json` (T.4) ; env-var `FORGE_EU_TIER` ABI to wrappers ; T3 case-block in fsm wrapper refuses Datadog + signoz.io + cloud-managed identity (Auth0/Okta/Keycloak-cloud) with structured `[REFUSAL: ...]` ; T1/T2 emit `[INFO: <tier>: ...]` only ; `<target>/.forge/.forge-tier` plain-text 1-line ledger (ADR-J8-006). Backward compat preserved (NFR-J8-002 — flag absence = no behavior). **(J.8.d)** CycloneDX 1.5 SBOM — `bin/forge-sbom.sh` bash thin + Python 3 inline (handcraft per Context7-verified mandatory fields, no external CycloneDX dep, NFR-J8-005), recursive lockfile detection (depth 4 + skip-list `node_modules/target/.dart_tool/.git/...`) for Cargo + npm-family + pubspec ; `SOURCE_DATE_EPOCH`-deterministic byte-identical output (FR-J8-075, uuid v5 derivation) ; new standard `global/sbom-policy.md` (NIS2/DORA/CRA rationale) ; new `sbom` job in `forge-ci.yml` uploads `sbom-cyclonedx` artefact. Smoke on `examples/forge-fsm-example/` → 74 components. Harness `j8.test.sh` 20/20 GREEN at `--level 1,2`. New consolidated spec `.forge/specs/janus-rules.md` (36 FRs `FR-J8-001..112` + 6 NFRs + 7 ADRs `ADR-J8-001..007`). J.8.c (`ai-native-rag` LLM gateway rules) deferred — depends on T7 `ai-native-rag` archetype not yet shipped. | T5 / J.8 ARCH §12.5 | Archived 2026-05-10 via `j8-janus-rules`   |
| **Done**       | **Flutter OTel standard v1.0.0 → v1.1.0 realign on real Workiva `opentelemetry: 0.18.11` (Q-004 follow-up to `t5-otel-app`). Delivered 2026-05-12 via `t5-otel-dart-api-realign`.** Documentation-only standards rewrite : `.forge/standards/flutter/opentelemetry.md` realigned to match the canonical pub.dev pkg actually consumed by `t5-otel-app` (verified 2026-05-11 via Context7 `/websites/opentelemetry_io` + WebFetch against `pub.dev/packages/opentelemetry/versions/0.18.11` + `github.com/Workiva/opentelemetry-dart`). v1.0.0 had been authored by cross-language transposition from JS/Java/Python OTel conventions and documented **9 fabricated symbols** that did not exist in the actual pkg. **Removed** : `OtlpHttpSpanExporter`, `OtlpHttpExporterConfig`, `BatchSpanProcessorConfig`, `TraceIdRatioBasedSampler`, `SpanStatusCode.{ok,error}`, `Context.current.withSpan(...)` method, `setStatus(..., message:)` named-arg, `package:opentelemetry/exporter_otlp_http.dart` sub-import, `package:opentelemetry/exporter_otlp_grpc.dart` sub-import. **Replaced by 7 verified symbols** : `CollectorExporter(Uri)`, `BatchSpanProcessor(exporter, {maxExportBatchSize, scheduledDelayMillis})`, `ParentBasedSampler(AlwaysOnSampler())`, `StatusCode.{ok,error}`, `contextWithSpan(Context, Span)` top-level fn, positional `setStatus(StatusCode, [String description])`, single bundled imports `package:opentelemetry/api.dart` + `sdk.dart`. New H2 `## Sampling` section documents the dual-stage Phase A (collector `processors.probabilistic_sampler` per `t5-otel-stack` ADR-OTEL-001) + Phase B (SDK `AlwaysOnSampler`) model because 0.18.11 does NOT ship a `TraceIdRatioBasedSampler` class. New Status callout at top : Workiva README `Traces: Beta / Metrics: Alpha / Logs: Unimplemented` — v1.1.0 explicitly scopes to traces. Frontmatter pins `pkg_version: 0.18.11`, `pkg_maintainer: Workiva`, `pkg_source`, `last_reviewed: 2026-05-11`. Phase A collector contract unchanged (HTTP/protobuf on `:4318`, parent-based sampler intent, W3C `traceparent` propagation, batch processor with 5 s flush). REVIEW.md gains append-only `Updated 2026-05-11` H2 entry (Article XII). Harness `.forge/scripts/tests/t5-otel-dart-api-realign.test.sh` 12/12 GREEN at `--level 1` (frontmatter + new API surface present + 9 legacy identifiers absent + REVIEW entry + workflow registration). Registered in `forge-ci.yml` matrix after `t5-otel.test.sh`. **Resolves `t5-otel-app::Q-004`** ; `_test_ota_l2_002_flutter_analyze` flip from xfail to GREEN is a separate follow-up commit on the `t5-otel-app` branch. **Follow-up for ARCHITECTURE-TARGET §9** noted : Workiva pkg `Traces: Beta / Metrics: Alpha / Logs: Unimplemented` asymmetry creates a 3-signal-completeness gap on the Flutter side relative to the Rust side — documentation-only awareness flag, no spec change in this archive. **J.7 scope gap noted as future follow-up** : `validate-standards-yaml.sh` covers `*.yaml` standards but NOT `flutter/*.md` documentation standards ; v1.1.0 frontmatter is documentation-greppable, not schema-enforced (deferred to a future linter extension change). | T5 / Q-004 follow-up to `t5-otel-app` | Archived 2026-05-12 via `t5-otel-dart-api-realign` (PR #8 merged)   |
| **Done**       | **T.5 Phase D — OTel live-run collector contract validation (additive, hermetic-by-default). Delivered 2026-05-12 via `t5-otel-live-run`.** Closes the T.5 OTel cross-layer story by adding a deterministic local-runner that proves a well-formed `ExportTraceServiceRequest` reaches the collector boundary carrying the expected `service.name` + W3C `traceparent` (ADR-T5-OTA-002 contract honoured). Hermetic-by-default (ADR-T5-OLR-002) : a Python-stdlib fake OTLP collector at `examples/forge-fsm-example/test/live-run/fake_otlp_collector.py` (varint + length-delimited tag walker per ADR-T5-OLR-001, ~340 LOC, no `protobuf`/`grpc`/`requests` pip dep) + a bash smoke driver `run_smoke.sh` posting two hex-canned probe payloads (direct `service.name=fsm-backend` ; kong adds `host.name=fsm-kong-gateway`) via Python `urllib`. Two committed golden captures `direct.golden.json` (35-byte payload) + `kong.golden.json` (68-byte payload) under `.forge/changes/t5-otel-live-run/captures/` with `captures/README.md` documenting determinism + sanitiser placeholders (`<ts:redacted>` / `<ip:redacted>` / `<host:redacted>` per ADR-T5-OLR-003). BDD feature `traceparent_live_run.feature` ships 2 Gherkin scenarios (direct + kong) referencing the Phase B `HeaderMapExtractor` symbol forward-pointer + cross-reference to Phase C `traceparent_e2e.feature`. Harness `.forge/scripts/tests/t5-otel-live-run.test.sh` **9/9 GREEN at `--level 1,2`** (8 L1 + 1 L2 docker leg opt-in via `FORGE_LIVE_RUN_DOCKER=1`, gracefully skipped in CI default). Registered in `forge-ci.yml` matrix after `t5-otel-traceparent-e2e.test.sh`. **Envoy gateway live-run deferred to T6 / B.8** per ADR-T5-OLR-005. **Zero `.rs` / `.dart` changes** (Phase B stands ; NFR-T5-OLR-004). New consolidated spec `.forge/changes/t5-otel-live-run/specs.md` (FR-T5-OLR-001..142 + 6 NFRs + 6 ADRs `ADR-T5-OLR-001..006`). | T5 / Phase D ARCH §12.1 + ADR-008 (collector contract) | Archived 2026-05-12 via `t5-otel-live-run`   |
| **Done**       | **Release script fix (F.3, pulled forward from T8). Delivered 2026-05-12 via `f3-release-script-fix`.** Maintainer-side release helper renamed `scripts/release-v0.3.0.sh` → `scripts/release.sh` (generic, accepts `--version X.Y.Z`). Two latent defects fixed : **bug 1** — the `run()` helper used bash `eval "$@"` which leaked cumulative `cd cli` operations into the parent shell ; the refactored helper wraps `eval` in a subshell `( eval "$@" )` AND the `npm publish` step uses an explicit `( cd cli && npm publish --access public --otp="$OTP" )` subshell. Working-directory invariant preserved : `pwd` at script exit equals `pwd` at script entry. **Bug 2** — `npm publish` 2FA OTP now plumbed via `--otp="$OTP"` (forwarded inside the publish subshell) ; OTP resolved through the ADR-F3-004 three-tier fallback chain : `--otp <6-digits>` flag (preferred for automation, regex `^[0-9]{6}$` validated) → interactive silent `read -rsp` prompt on a TTY (preferred for manual runs) → `$NPM_OTP` environment variable (preferred for CI / scripted contexts). Script exits 2 with a clear message if all three sources are absent and publish is not being skipped. **OTP value never echoed or logged** ; dry-run trace redacts to `<redacted>` ; rejected OTP values are printed as `<redacted>` in error messages. Four ADRs (ADR-F3-001..004) resolve : old script deletion (no symlink — the new script's required `--version` flag makes a symlink fail with the same usage error a missing-file error produces) ; `cli/assets/scripts/` adopter template out of scope (verified 2026-05-12 — does not exist in the current tree) ; flag form `--version <X.Y.Z>` over `--bump <level>` (the maintainer pins VERSION + CHANGELOG manually at archive time ; the script verifies, it does not decide) ; OTP fallback chain order + fail-loudly semantics. **`GOVERNANCE.md § Release Process`** updated to document the new invocation form. Harness `f3.test.sh` **10/10 GREEN at `--level 1`** ; 1 L2 opt-in fixture (`FORGE_F3_LIVE=1`) drives the script in `--dry-run` mode + asserts the OTP literal is redacted from the trace ; registered in `forge-ci.yml` `harness` matrix after `i5.test.sh` with `--level 1` (286 lines, well under the NFR-CI-002 300-line budget). verify.sh OVERALL PASS preserved (no regression) ; constitution-linter.sh OVERALL PASS preserved. No constitutional amendment ; Articles III.4, V, XII compliance preserved. New consolidated spec `.forge/changes/f3-release-script-fix/specs.md` (FR-F3-001..127 + 7 NFRs + 4 ADRs ADR-F3-001..004). | T8 / F.3 (pulled forward) | Archived 2026-05-12 via `f3-release-script-fix`   |
| **Done**       | **CLI v0.3.2 — three first-experience bug fixes against the published v0.3.1 tarball. Delivered 2026-05-13 (no spec / standard / constitution change).** Three regressions surfaced when `forge init` was exercised against the **published binary** (not source tree) on a fresh machine, none caught by the existing `cli/test/e2e/cli.test.ts` because it never invokes the bundled-tarball binary against a non-existent target / wizard-less archetype path : **(fix 1)** `--eu-tier <T1\|T2\|T3>` was declared by J.8 in `cli/src/commands/init.ts` (`EU_TIER_ENUM` + validator + env-var propagation) but never wired into commander in `cli/src/cli.ts` — users hit `error: unknown option '--eu-tier'` on v0.3.0 / v0.3.1. Now wired (`.option("--eu-tier <tier>", ...)` + `euTier?: string` field + propagation), regression covered by a new e2e test asserting `forge init --help` lists `--eu-tier` ; **(fix 2)** `forge init --target <new-dir>` failed with `spawn bash ENOENT` when `--target` pointed to a path that did not yet exist — the bash scaffolder does `mkdir -p` internally at line 184 but that runs after `spawn`, which itself fails when `cwd` does not exist. Fix : `cli/src/commands/init-archetype.ts:148` now does `await mkdir(opts.targetDir, { recursive: true })` immediately before the runner call (idempotent, no-op when present) ; **(fix 3)** `forge init` required `--force` even in an empty target dir — `.forge/scripts/scaffolder/init.sh:168` previously refused to scaffold when the target dir merely existed regardless of contents, so the natural `mkdir foo && cd foo && forge init …` hit the collision guard on a freshly-created empty dir. Fix : the check now refuses only when the dir exists AND is non-empty (`ls -A` skips `.` / `..`) ; empty dirs proceed without `--force`. **Triggers T5.1 — CLI Trust Harness** (planned, pre-v0.3.3 ; cf. roadmap entry below) : the three regressions share a single root cause (no test exercises the published binary against a real scaffold rendered fresh in a clean tmpdir), addressed by the four T5.1 layers. | v0.3.0 / v0.3.1 release post-mortem | Shipped 2026-05-13 in `@sdd-forge/cli@0.3.2`   |
| **Done**       | **T5.1 — CLI Trust Harness + tactical fix-forwards — fully archived 2026-05-16 (3 changes : `cli-trust-harness` + `t5-cargo-pin-refresh` + `t5-bin-server-deps`). Released as `@sdd-forge/cli@0.3.3` post-archive.** Four core layers wrap `npm publish` to prevent the v0.3.0 → v0.3.2 pattern from recurring. First run of `task validate` (2026-05-16) surfaced three real framework bugs : (1) `buffa = "=0.3.3"` Cargo pin dead (crates.io has 0.5.x/0.6.0) → handled by new change `t5-cargo-pin-refresh` (T5.1.E) ; (2) `opentelemetry_sdk` package fantôme in mobile-only template ; adjacent investigation revealed Workiva `opentelemetry: 0.18.11` is **web-only** → escalated out of T5.1 to **T5.3** (`t5-otel-dartastic-realign`, separate row below) ; (3) cosmetic harness label leak via bash dynamic scoping → patched inline as T5.1.F before archive. T5.1 release criterion v0.3.3 now requires both `cli-trust-harness` AND `t5-cargo-pin-refresh` archived. Detailed in `docs/new-archetypes-plan.md` §0.1 (original + extension subsection) / §1.4 / §11. **Layer T5.1.0 — Fixed `Taskfile.yml.tmpl:67`** (`echo "infra tests: delegated to b1-delivery workflows"` is parsed as a YAML mapping `{"echo \"infra tests": "delegated to b1-delivery workflows\""}` by go-task because `: ` mid plain-scalar is a key/value separator — single-quote sweep across all templates). **Layer T5.1.A — Golden snapshot of CLI flags** (`cli/test/e2e/help-snapshots.test.ts` captures `forge {init,upgrade,verify,version} --help` ; any surface drift forces snapshot update + review ; cross-check each non-`removed_from_roadmap` archetype from `dispatch-table.yml` appears in `forge init --help`). **Layer T5.1.B — Smoke test per archetype** (`cli/test/e2e/archetypes-smoke.test.ts` iterates dispatch-table archetypes, scaffolds in a non-existent `mkdtemp` to exercise the v0.3.2 `spawn bash ENOENT` fix, asserts an expected-files matrix declared in `cli/test/e2e/archetype-fixtures/<name>.yml`, runs `task --list-all` on the scaffolded project as YAML+command validation, skip-pass `task`/`flutter`/`cargo` if absent, opt-in tighter checks via `FORGE_E2E_TOOLCHAINS=1`). **Layer T5.1.C — Pre-publish tarball gate** (`cli/scripts/prepublish-smoke.mjs` does `npm pack` → extract to isolated tmpdir → install globally via isolated `prefix` → re-runs T5.1.B against the **installed binary**, wired into `package.json::prepublishOnly`). **Layer D differé à T6 / B.8.15** — `forge upgrade` matrix test pour chaque paire N-1 → N (cf. roadmap Phase 3 detail row B.8). No new standard / agent / constitution amendment. **Release criterion v0.3.3** : T5.1 archived + all four layers GREEN in CI. | Issue post-v0.3.0 / v0.3.1 / v0.3.2 release pain | Planned 2026-05-14 (pre-v0.3.3 patch release)  |
| **Done**       | **T5.2 — Anti-Hallucination Platform Verification (process change, 2026-05-16 → implemented 2026-05-18).** Delivered via `t5-2-platform-verification` (status `implemented`, ready to archive ; release target v0.3.4 patch). Article III.4 (Ambiguity Protocol — anti-hallucination) reinforced procedurally, no constitution amendment. Four deliverable clusters shipped : (A) Forge-local `.claude/agents/document-specialist.md` override with H2 `## Platform Verification Checklist (3-axis)` carrying axis names + MUST tick-all + `[PLATFORM MISMATCH:]` escalation token + Q-006 worked example + scope clarification + Article III.4 xref ; (B) `.forge/standards/global/standards-lifecycle.md` additive bump v1.0.0 → v1.1.0 with new H2 `## Platform compatibility re-verification` codifying cadence (SHOULD at 12-month review / MUST on archetype target-platform addition / MUST pre-ratification of any external dep-pinning standard) + frontmatter introduced explicitly (was authored pre-J.7 without frontmatter — v1.0.0 was implicit) ; (C) REVIEW.md append-only ledger entry 2026-05-18 (Article XII discipline) ; (D) harness `.forge/scripts/tests/t5-2.test.sh` 8 L1 grep + 1 L2 opt-in `FORGE_T52_LIVE=1` (pub.dev tooling smoke on `flutter_bloc` per ADR-T52-002) ; CI matrix registered after `t5-otel-live-run.test.sh` ; docs/CONTRIBUTING.md + docs/LINTING.md cross-references added (Informative rule, non-enforced) ; CHANGELOG.md [Unreleased] entry. Drift guard ADR-T52-003 enforced : both H2 titles cited verbatim across 4 reinforcing surfaces. Harness 8/8 L1 GREEN + 9/9 with L2 live ; wall-clock 0.06s L1 / 0.31s L1+L2 (NFR-T52-002 budget 5s/15s respected ; 78×/49× under). `verify.sh` 253/0 + `constitution-linter.sh` PASS + `t5-1.test.sh::ci_line_budget` 17/17 (forge-ci.yml 298 ≤ 300 after compaction of one neighbouring comment block). **Validation meta** : an independent code-reviewer pass caught a fabricated constitutional citation ("Article VIII" instead of "Article III.4") embedded in the spec/design/agent body — exactly the class of bug T5.2 exists to prevent ; corrected pre-archive across 14 occurrences in 6 surfaces. Detailed in `docs/new-archetypes-plan.md` §0.2. | Q-006 (Workiva web-only platform mismatch discovered 2026-05-16 by `task validate`) | Implemented 2026-05-18 (pre-v0.3.4) |
| **Done**       | **T5.3 — `t5-otel-dartastic-realign` (target v0.4.0-rc.1, implemented 2026-05-18).** Inaugural application of T5.2 3-axis platform-verification checklist on 3 Dartastic packages (`dartastic_opentelemetry_api ^1.0.0-beta.2` transitive + `dartastic_opentelemetry ^1.1.0-beta.6` + `flutterrific_opentelemetry ^0.4.0`). `flutter/opentelemetry.md` v1.1.0 → **v2.0.0** breaking bump with WAIVER block (ADR-T53-002 ; upgrade trigger `t5-3-1-dartastic-api-ga-refresh`). FSM frontend 5 Dart files + mobile-only template 4 .tmpl rewritten on Dartastic ; cli/assets mirror preserved. 3 archived changes carry `.forge-update-notes` forward-pointers (Article V immutability). Harness `t5-otel-dartastic.test.sh` 13 L1 + 2 L2 opt-in (`FORGE_T53_LIVE=1` — `flutter pub get` + `flutter analyze` on FSM + fresh mobile-only scaffold) ; **L2 GREEN sur les 2 archétypes via `task validate / smoke-with-toolchains`** (3/3 PASS en 34.5 s). Independent code-reviewer agent caught 2 HIGH findings pre-archive (`FlutterrificOTel.routeObserver()` fabricated → `FlutterOTel.routeObserver` getter ; pin `^0.9.5` stale → `^1.1.0-beta.6`). 7 fix-forward iterations on Dartastic API symbol-level errors (only caught by `flutter analyze`). **First fixes carried in T5.3** (externalités positives — bugs pré-T5.3 masked by upstream Workiva pub-get failure) : mobile-only analyzer dette 14 issues fixed (unused imports, sort_constructors_first ×8, withOpacity → withValues, prefer_const) ; gherkin dead weight removed from mobile-only pubspec.yaml.tmpl (uuid ^3 vs ^4 transitive conflict with flutterrific) ; Taskfile root mvdan/sh quirks fixed (3 patches : array+default expansion, trap signal names, length test) ; .env bootstrap added in `dev-up-matrix` (cp .env.example .env before task dev:up) ; `archetypes-smoke.test.ts` failure message surfaces flutter analyze stdout (issues) in addition to stderr (summary). **Remaining red light deferred to T5.3.1** (`b1-1-dev-up-matrix-fixes`, separate row below) : `image: scratch` placeholder + `version` obsolete attribute in `full-stack-monorepo/docker-compose.dev.yml.tmpl` — pre-existing b1 template hygiene, unrelated to Workiva→Dartastic substitution. Detailed in `docs/new-archetypes-plan.md` §0.3. **Release target** : v0.4.0-rc.1 (pre-GA minor bump per VERSIONING.md). | Q-006 architectural bug + Workiva web-only platform | Implemented 2026-05-18 (pre-v0.4.0-rc.1) ; Archived 2026-05-26 via `t5-otel-dartastic-realign` (consolidated spec `.forge/specs/otel-dartastic-realign.md`) |
| **Done**       | **T5 Phase C — `t5-otel-traceparent-e2e` (W3C traceparent end-to-end validation through Kong gateway). Implemented 2026-05-11, archived 2026-05-26.** Phase C of the T.5 OTel rollout — Phase A (`t5-otel-stack`) shipped infra ; Phase B (`t5-otel-app`) shipped SDK + middleware ; this Phase C ships the BDD scenario text + Kong preservation contract + the harness gate. New `examples/forge-fsm-example/test/features/traceparent_e2e.feature` (3 scenarios — Direct path, Kong path, Sampled-off path) coexisting with the Phase B `demo_005_traceparent.feature` stub. Symbol-name forward-pointers cite Phase B's actual exported carriers verbatim (`HeaderMapExtractor`, `MetadataMapCarrier`, `HeaderMapCarrier`). Kong gateway `kong.yml.example` audited and gains a contractual comment block stating that Kong MUST forward incoming `traceparent` and `tracestate` headers verbatim (no `request_transformer.remove.headers` for those headers). New harness `.forge/scripts/tests/t5-otel-traceparent-e2e.test.sh` ships 7 L1 grep-based tests (feature file presence + audit header, three scenarios, Given/When/Then discipline, symbol forward-pointer, Kong no-traceparent-strip, Kong contract comment, CI matrix entry) + 2 L2 tests (cargo build inherited, flutter analyze xfail inheritance Q-004 cascade — comment phrasing matches Phase B). Registered in `forge-ci.yml` `harness` matrix immediately after `t5-otel-app.test.sh` with `--level 1`. **Zero production-code edits** (NFR-T5-TPE-004 — no `.rs`/`.dart`/`.proto`/`Cargo.toml`/`pubspec.yaml` touched ; comment-only on `.yml.example`). Phase D — DEFERRED section in `tasks.md` enumerates live-run leg deliverables (`docker compose up` driver, stub OTLP receiver, programmatic traceId assertion, SigNoz API verification, Envoy migration validation handled by future `b8-envoy-migration`). New consolidated spec `.forge/specs/otel-traceparent-e2e.md` (27 FRs `FR-T5-TPE-001..091` + 7 NFRs `NFR-T5-TPE-001..007` + 3 Article II Gherkin scenarios verbatim). Q-001 (sampled-off SDK behaviour) + Q-002 (Envoy forward-pointer name = `b8-envoy-migration`) resolved at `/forge:design` time. | T5 / OTel app ARCH Phase C + ADR-008 | Archived 2026-05-26 via `t5-otel-traceparent-e2e` (consolidated spec `.forge/specs/otel-traceparent-e2e.md`) |
| **Planned**    | **T5.3.1 — `b1-1-dev-up-matrix-fixes` (template hygiene, planned 2026-05-18).** Closes the residual `dev-up-matrix` red lights that T5.3 exposed but were not part of the Workiva → Dartastic substitution scope. **T5.3.1.A** : fix `image: scratch` placeholder in `full-stack-monorepo/docker-compose.dev.yml.tmpl:60` (Docker rejects "scratch" as reserved name when used as `image:`) — option to retain : working stand-in image (e.g. `nginx:alpine` with healthcheck shim) OR Docker Compose `profiles: ["backend"]` opt-in. **T5.3.1.B** : drop obsolete `version: "3.X"` attribute from the same docker-compose template (`the attribute version is obsolete` Compose v2 warning). **T5.3.1.C** : add positive smoke assertion in `archetypes-smoke.test.ts` (or new e2e suite) exercising `task dev:up` + `docker compose ps` + `task dev:down` cycle on the scaffolded project. **T5.3.1.D** : `task validate` GREEN end-to-end on both archetypes as the release criterion. Pre-existing `b1-foundations` / `b1-delivery` template hygiene ; **never exercised end-to-end** before T5.3 because the `dev-up-matrix` Taskfile + upstream Workiva pub-get + mobile-only `flutter analyze` were RED first. Now that T5.3 fixed all 3 upstream layers, the downstream template bug becomes visible. Effort `S`–`M` (~half-day to 1 day). Release : piggyback v0.4.0-rc.1 if T5.3.1 lands before tag, OR separate patch `v0.4.0-rc.2`. Détails `docs/new-archetypes-plan.md` §0.4. | Pre-existing b1 template hygiene exposed by T5.3 dev-up-matrix chain | Planned 2026-05-18 (v0.4.0-rc.1 or rc.2) |
| **Done**       | **OTel App SDK instrumentation in flagship example (T.5 Phase B, app side, additive). Delivered 2026-05-12 via `t5-otel-app`.** Phase B of the T.5 OTel rollout — Phase A (`t5-otel-stack`) shipped the infra side ; this change wires the application-side SDK init in both layers of `examples/forge-fsm-example/` so demo-005-connect-greeting produces a connected span tree in SigNoz with a single `traceId`. **Rust backend** : new `crates/infrastructure/src/telemetry/` (`mod.rs`, `propagation.rs`, `middleware.rs`) with `setup_telemetry`, `TelemetryConfig::from_env()`, OTLP HTTP/protobuf exporter (`with_protocol(Protocol::HttpBinary)`, port 4318, ADR-T5-OTA-002), 4 mandatory `Resource` attributes (`service.name`, `service.version`, `deployment.environment`, `host.name`), `ParentBased(TraceIdRatioBased(rate))` sampler (ADR-T5-OTA-003), `tracing-opentelemetry` bridge, `tower-http::TraceLayer::new_for_http()` outermost with `make_span_with` extracting W3C `traceparent` via `TraceContextPropagator`, `MetadataMapCarrier` (gRPC) + `HeaderMapCarrier` (outbound HTTP). Crate pins per ADR-T5-OTA-001 : `opentelemetry 0.31` family + `tracing-opentelemetry 0.32` + `tower-http 0.6 [trace]`. **Flutter frontend** : new `lib/core/telemetry/` (`telemetry_setup.dart`, `observers/tracing_navigation_observer.dart`, `observers/tracing_bloc_observer.dart`, `error_reporter.dart`, `interceptors/tracing_interceptor.dart`) + `lib/core/config/app_config.dart` + rewritten `lib/main.dart` per ADR-T5-OTA-005 init order. `pubspec.yaml` gains `opentelemetry: ^0.18.0` + `dio: ^5.7.0` (T-VER-DART-001 deferred-pin ; `flutter pub get` resolves `0.18.11` + `5.9.2`). **Q-004 cascade resolved** : the v1.0.0 `flutter/opentelemetry.md` standard documented a fabricated API surface (cross-language transposition from JS / Java / Python OTel) ; sibling change `t5-otel-dart-api-realign` (commit `280e39e`) bumped the standard to **v1.1.0** ratifying the actual `opentelemetry: 0.18.11` (Workiva) public API ; this change's Q-004 follow-up commit (`15b774c`) realigned the example Dart code to v1.1.0. The `_test_ota_l2_002_flutter_analyze` smoke flipped from xfail to expected-pass. **demo-005 traceparent round-trip** : Greeter use case carries `#[tracing::instrument(...)]` ; Flutter `GreetingRepositoryImpl` builds a `Dio` client with `TracingInterceptor` pre-attached. **Test harness** : 16 L1 + 2 L2 ; **18/18 GREEN** at `--level 1,2`. New consolidated spec `.forge/specs/otel-app.md` (56 FRs + 7 NFRs + 7 ADRs `ADR-T5-OTA-001..007`). | T5 / OTel app ARCH §12.1 + ADR-008 Phase B | Archived 2026-05-12 via `t5-otel-app` (depends on `t5-otel-dart-api-realign`) |

## Phase 3 (Post-v0.3.0 — 2026-Q3 to 2026-Q4) — Flagship migration + new archetypes

This phase ratifies the **ten ADRs** of `docs/ARCHITECTURE-TARGET.md`
(2026-04-29, rev v1.1) and reorganises the archetype taxonomy from 4 to 5.
The full plan lives in `docs/new-archetypes-plan.md`. Headline shifts:

- **Taxonomy reshuffled** : `flutter-firebase` is **removed** (Schrems II +
  CLOUD Act), `mobile-only` is **renamed** `mobile-pwa-first` (PWA Qwik +
  iOS native fallback), two new archetypes added — `event-driven-eu`
  (NATS + Temporal + AsyncAPI) and `ai-native-rag` (pgvector + LLM gateway
  + MCP). `full-stack-monorepo` and `rust-cli-tui` kept.
- **Flagship technical migration** : Kong → Envoy Gateway, Temporal → DBOS
  (Postgres-backed durable execution), REST/JSON Kong-bridge → Connect-RPC,
  Firebase implicit → Zitadel, Flutter Web public → Qwik (back-office
  Flutter Web kept), Postgres 17 + pgvector universal default, SigNoz +
  OBI eBPF + Coroot triplet observability. Schema bumps `1.0.0 → 2.0.0`.
- **flutter_bloc consecrated** as the only allowed Flutter state-management
  standard. Riverpod / Provider / GetX / MobX / states_rebuilder are
  **forbidden** by a CI-blocking linter rule (`no-state-management-alternatives`).
- **Compliance EU graded T1/T2/T3** introduced as a first-class dimension
  (T1 RGPD via DPA acceptable, T2 self-hostable, T3 SecNumCloud / EUCS
  High strict EU jurisdiction).
- **Five new agents** : Hermes-Async (event-driven), Pythia (AI/RAG),
  Demeter (data steward EU), Iris-Web (Qwik / SvelteKit), Themis (compliance
  officer NIS2 / DORA / CRA).

### Phase 3 Items — overview by quarter

| Quarter | Modules                                                                                              | Effort  | Risk     |
|---------|------------------------------------------------------------------------------------------------------|---------|----------|
| **T4** (2026-Q3 early) | **Done 2026-05-04 via `t4-adr-ratification`** : P-1 (10 ADRs consolidated as 1 archived change rather than 10 — Article V audit trail preserved through `FR-T4-ADR-001..010`) ; P-2 / J.1–J.6 (six `.forge/standards/*.yaml` v1.0.0 shipped) ; P-3 (`global/standards-lifecycle.md` + `REVIEW.md` ledger) ; P-4 / I.1 (`compliance-tier.schema.json` + `archetype.schema.json` v2). **P-5 (Hera 9 → 5) retired 2026-05-06 — rejected as unsourced architect opinion ; 9 sub-agents stay.** | `M` (delivered) | — |
| **T5** (2026-Q3) | **Phase 1 Done 2026-05-06** via `t5-connect-codegen` (PR #3). **J.7 Done 2026-05-08** via `j7-validate-standards-yaml` (PR #4 merged). **OTel + OBI + Coroot stack templates Done 2026-05-10** via `t5-otel-stack` (PR #5 merged — 14/14 tests, +6 PASS verify.sh, snapshot 520 KB, `observability.yaml` 1.0.0 → 1.1.0). **J.8 Done 2026-05-10** via `j8-janus-rules` (PR #6 merged) — Janus forbidden-archetypes refusal (J8-RULE-001..003, exit 3 + `[REFUSAL: ...]` stderr), `--eu-tier T1\|T2\|T3` flag plumbing, T3 enforcement in fsm wrapper, CycloneDX 1.5 SBOM via `bin/forge-sbom.sh`, 2 new standards (`global/janus-orchestration-rules.md` + `global/sbom-policy.md`) ; 20/20 tests, +6 PASS verify.sh, smoke 74 components on flagship example. **K.3 Done 2026-05-12** via `k3-demeter` (PR #7 merged) — Demeter data-steward persona (`.claude/agents/demeter.md`), deterministic dependency scanner `bin/forge-demeter-scan.sh` (F.2 / J.7 / J.8.d pattern verbatim), `.forge/data/cloud-act-publishers.yml` deny-list, standard `global/data-stewardship-rules.md`, K3-RULE-NNN namespace, Janus delta to Step 9 (`Security & Data-Stewardship Pass (Aegis + Demeter)`), DPA ledger `.forge/.forge-dpa-declared` mirroring J.8 ADR-J8-006 (ADR-K3-002, resolves Q-001) ; 22/22 tests `k3.test.sh --level 1,2`, verify.sh + constitution-linter PASS. **OTel App SDK instrumentation Done 2026-05-12** via `t5-otel-app` (PR #9 merged ; Phase B — app-side SDK init in `examples/forge-fsm-example/` both layers, demo-005 traceparent round-trip, 18/18 GREEN at `--level 1,2`, **Q-004 cascade resolved** via sibling `t5-otel-dart-api-realign`). **I.2 Done 2026-05-12** via `i2-compliance-tiers` — single human-readable standard `global/compliance-tiers.md` v1.0.0 codifying T1 / T2 / T3 (437 LOC, 7 H2 sections, verbatim citation of `compliance-tier.schema.json::x-tier-descriptions` + 15-row component matrix byte-identical to `ARCHITECTURE-TARGET.md` §10.2, frontmatter pins `linter_rule: t3-forbidden-components` forward-pointer to I.3, 5 MUST NOT clauses), index registration (9 triggers, scope all, priority high), REVIEW.md birth entry, `docs/COMPLIANCE.md` adopter intro at root with decision tree, 14/14 L1 tests `i2.test.sh`. Resolves the K.3 / Demeter forward-pointer (`.claude/agents/demeter.md::Standards consumed`). **I.6 Done 2026-05-12** via `i6-compliance-artefacts` — deterministic `.tgz` regulatory hand-off bundle generator `.forge/scripts/compliance/bundle.sh` (bash thin + Python 3 inline mirroring `bin/forge-sbom.sh` per NFR-I6-CA-004) + DPA template `.forge/templates/compliance/forge-dpa-declared.template` + standard `global/compliance-artefacts-bundle.md` v1.0.0 (6 H2 + 4 MUST NOT, forward-stable schema). Six bundle members (MANIFEST + tier-matrix + dpa-template + audit-ledger ×2 + CycloneDX SBOM). `SOURCE_DATE_EPOCH` determinism asserted by L2 fixture (NFR-I6-CA-005, byte-identical via `diff -q`). 16/16 tests `i6.test.sh --level 1,2`. Three ADRs (ADR-I6-CA-001..003 — archive format / ledger placement / script location). Bundle layout forward-stable for Themis-territory artefacts (NIS2 / DORA / CRA / AI Act). **I.3 Done 2026-05-12** via `i3-t3-forbidden-linter` — `constitution-linter.sh` ADR-I3-001 section + standard `global/forbidden-components-rules.md` v1.0.0 (10 seed rules `T3-RULE-001..010`) + tier-scaled severity (T1/T2 warn Phase A → T3 fail immediate, mirrors J.8) + 14/14 L1 tests `i3.test.sh` ; resolves I.2 `linter_rule:` forward-pointer ; unblocks I.5. **I.5 Done 2026-05-12** via `i5-compliance-workflow` — reusable `.github/workflows/forge-compliance.yml` (158 LOC, `on: workflow_call:` with 3 inputs / 1 output) orchestrant Demeter + linter + SBOM + bundle ; upload `.tgz` via `actions/upload-artifact@v4` ; standard `global/forge-compliance-workflow.md` v1.0.0 (284 LOC, 7 H2 + 4 MUST NOT + 8 triggers) ; index entry + REVIEW birth + `docs/COMPLIANCE.md` H2 + CHANGELOG ; 17/17 tests `i5.test.sh --level 1,2` (16 L1 + 1 L2 skip-pass) ; trois ADRs ADR-I5-CW-001..003 (exit-code aggregation trust-each-script + SBOM no-lockfile non-fatal ; `SOURCE_DATE_EPOCH` commit-ts source ; L2 act-opt-in `FORGE_I5_ACT=1` mirroring `t5-otel-live-run`) ; resolves I.6 `compliance-artefacts-bundle.md::Consumption protocol` forward-pointer. **Phase D Done 2026-05-12** via `t5-otel-live-run` — hermetic Python-stdlib fake OTLP collector + smoke driver + 2 golden captures + BDD feature ; 9/9 GREEN at `--level 1,2` (L2 docker leg opt-in via `FORGE_LIVE_RUN_DOCKER=1`) ; Envoy gateway live-run deferred to T6 / B.8 per ADR-T5-OLR-005. **T5.3 Done 2026-05-18** via `t5-otel-dartastic-realign` (released as `v0.4.0-rc.1` 2026-05-19) — Workiva → Dartastic OTel substitution, `flutter/opentelemetry.md` v1.1.0 → v2.0.0 breaking, FSM frontend + mobile-only template rewritten, 13/13 L1 GREEN, inaugural T5.2 3-axis checklist application. **T5.3.1 Done 2026-05-19** via `b1-1-dev-up-matrix-fixes` — `full-stack-monorepo` `docker-compose.dev.yml.tmpl` hygiene : `image: scratch` → `traefik/whoami:latest --port 8080` (ADR-B1-DUM-001), top-level `version: "3.8"` removal (ADR-B1-DUM-002), new harness `t5-3-1.test.sh` (9 L1 + 1 L2 opt-in `FORGE_B1DUM_DOCKER=1` per ADR-B1-DUM-003), 4-copy mirror sync + snapshot regen, `forge-ci.yml` 299/300 ; release target `v0.4.0-rc.2`. **T5.3.2 ABANDONED 2026-05-20** — `t5-otel-stack-image-refresh` scope was "pin refresh" but `docker manifest inspect` empirical verification (Article III.4) revealed SigNoz did a full architectural migration : 3-services (`signoz/frontend` + `signoz/query-service` + collector) → 1 unified image (`signoz/signoz` + `signoz/signoz-otel-collector`). Old archi fully gone from Docker Hub (even the last published 0.76.3 is rotted). Pin refresh is technically impossible ; the fix is re-architecture which is **explicitly scope-out** of T5.3.2 and belongs to **B.8 / T6** (flagship 1.0.0 → 2.0.0 observability stack re-architecture per ADR-008). v0.4.0-rc.2 ships T5.3.1 only ; `task validate` `dev-up-matrix` stays RED on main as a known-issue until B.8 lands. Investigation evidence preserved in `docs/new-archetypes-plan.md` §0.5 so the B.8 architect doesn't re-discover the dead-end. **T5.3.3 Done 2026-05-20** via `t5-3-3-vitest-bundle-preflight` — `cli/test/global-setup.ts` runs `npm run bundle` once before any vitest suite (via `cli/vitest.config.ts::test.globalSetup`), closing the bypass where `npx vitest run` skipped the bundle and the e2e suite rsynced from a stale `cli/assets/` mirror (T5.3.1 reviewer LOW finding reproduced 2026-05-20). New harness `t5-3-3.test.sh` (5 L1, no L2 — inverse proof intrinsically covered by e2e). ADRs T533-001..002 (spawnSync + CI comment compression) resolved inline ; 294/300 forge-ci.yml. Release target v0.4.0-rc.3 or v0.4.0 final. **Still pending in T5** : NIS2/DORA/CRA/AI Act regulatory-deadline artefacts under `.forge/compliance/` (Themis K.5, T7+), traceparent W3C E2E validation through Envoy/Kong (deferred with the L2 fixtures to T6 / B.8). | `L` | Low — additive only, fully reversible |
| **T6** (2026-Q4 early) | **B.8 — flagship migration `1.0.0 → 2.0.0`** (Envoy Gateway, DBOS, Connect-RPC, Zitadel, Postgres+pgvector, SigNoz+OBI+Coroot) ; Phase 2 ARCHITECTURE-TARGET ; **B.8.15 — T5.1 Layer D `forge upgrade` matrix test** (covers each N-1 → N pair starting with the critical 1.0.0 → 2.0.0 flagship migration, asserts `upgrade_history` ledger + smoke T5.1.B re-run on upgraded tree + `[NEEDS MIGRATION:]` negative path). **B.8.8 (Coroot leg) Done 2026-05-25 via `b8-coroot-rehost`** — pilot of the `b8-observability-rearch` trio (Coroot → SigNoz → OBI). Host migration `docker.io/coroot/coroot:1.4.4` → `ghcr.io/coroot/coroot:1.20.2` (docker.io public access denied 2026-05-24, verify-then-pin lesson T5.3.2). `observability.yaml` v1.1.0 → v1.2.0 additive ; 4-copy mirror sync ; new harness `b8-coroot.test.sh` 13/13 GREEN ; 4 ADRs `ADR-B8-COR-001..004` (uniform no-v-prefix `versions.*` — inverted at impl after L2 manifest-pull caught the proposal's v-prefix-mandatory mis-read ; no `coroot_registry` field ; ConfigMap diff CHANGELOG+L2 manifest-pull ; T1/T2 OK + T3 candidate-substitution flag). Siblings `b8-signoz-unified` + `b8-obi-refresh` follow. **B.8.8 (SigNoz leg) Done 2026-05-27 via `b8-signoz-unified`** — trio sibling 2. SigNoz 3-service → unified arch migration : `docker-compose.dev.yml.tmpl` rewritten to 6 services (4 long-running `signoz/signoz:v0.125.1` + `signoz/signoz-otel-collector:v0.144.4` + `clickhouse/clickhouse-server:25.5.6` + `signoz/zookeeper:3.7.1`, + 2 init `init-clickhouse` + `fsm-signoz-telemetrystore-migrator`) ; UI `${SIGNOZ_UI_PORT:-3301}` → ctr `:8080` (ADR-B8-SIG-004) ; OPAMP-off dev (ADR-B8-SIG-003). `observability.yaml` v1.2.0 → v2.0.0 **BREAKING** (versions surgery + ISO 8601 `pin_review_cadence:` + WAIVER citing ADR-J7-004 + `breaking_change: true`) ; REVIEW.md **ARCH-CHANGE** ledger flag (first use, FR-B8-SIG-H-006) ; 6-copy mirror sync ; new harness `b8-signoz.test.sh` 17/17 L1 + 6 L2 opt-in ; `forge-ci.yml` 300/300 ; snapshot regen ; `a7.test.sh` 29/29 ; débloque le `task validate dev-up-matrix` RED rc.2/rc.3. 7 ADRs `ADR-B8-SIG-001..007`. Released `v0.4.0-rc.4` 2026-05-28. **B.8.8 (OBI leg) Done 2026-05-29 via `b8-obi-refresh`** — trio sibling 3, closes the trio. Beyla 2.0.1 → 3.15.0 major bump (multi-arch amd64+arm64 verified live `docker manifest inspect`) ; `observability.yaml` v2.0.0 → v2.1.0 additive minor (`breaking_change: false` ; `pin_review_cadence.beyla: "P12M"` preserved ; sibling 2 WAIVER block preserved Article V) ; REVIEW.md ledger appended with `Updated` flag (NOT `ARCH-CHANGE`) ; ClusterRole RBAC widened `services` resource per Beyla 3.x docs (read-only ; ADR-B8-OBI-003) ; caps + kernel-58 UNCHANGED per Beyla 3.x docs (ADR-B8-OBI-002/004) ; 4-copy mirror sync (no example-side `.forge/templates/...` mirror — K8s-only, opposed to SigNoz 6-copy) ; new harness `b8-obi.test.sh` 22/22 L1 + 2 L2 opt-in `FORGE_B8_OBI_DOCKER=1` ; sibling-harness coupling break hybrid (ADR-B8-OBI-006 narrow `t5-otel:128/233` + widen `b8-coroot:169/196` + `b8-signoz:229/299/304/323` date/version/breaking_change regex windows) ; `forge-ci.yml` 300/300 preserved via 3-comment compression per ADR-T533-002 ; snapshot 675088 B (700 KiB ceiling preserved with ~42 KB headroom) ; `a7.test.sh` 29/29 ; 8 ADRs `ADR-B8-OBI-001..008` ; Q-001..007 all resolved at `/forge:design` via live `docker manifest inspect` + Context7 `/grafana/beyla`. Release target `v0.4.0-rc.5` (trio closure). **B.8.1 Done 2026-05-30 via `b8-1-audit-baseline`** — flagship 1.0.0 baseline audit ; `docs/B8-BASELINE.md` + `.forge/baselines/` span-inventory + `b8-1.test.sh` 10 L1 + 1 L2 ; independent review APPROVE ; v0.4.0-rc.6 released. **B.8.2 Done 2026-05-30 via `b8-2-legacy-snapshot`** — 1.0.0 maintenance-freeze + sha256 immutability guard `b8-2.test.sh` 4 L1 ; `upgrade-policy.md` freeze section + REVIEW ledger ; independent review APPROVE ; v0.4.0-rc.7 target. **B.8.3 Done 2026-05-30 via `b8-3-schema-candidate`** — 2.0.0 candidate target schema `2.0.0.yaml` ; 15 FRs + 5 NFRs ; frozen-1.0.0-safe, reference-only, gating B.8.4–B.8.12 ; `b8-3.test.sh` 17/17 L1 ; independent review APPROVE ; archived 2026-05-30. **B.8.3.b proposed** — validator versioned-schema discovery (rewire `validate-foundations.sh` / `verify.sh` / `constitution-linter.sh` to discover + validate versioned schema filenames e.g. `2.0.0.yaml` ; enables enforcing `scaffoldable:false`) ; proposed by b8-3 design, effort S, not yet committed. | `XL` | **High — point of no return.** Canary by route, blue-green Envoy/Kong, Temporal/DBOS dual-run during migration. B.8.15 closes the last T5.1 layer once 2.0.0 snapshot tarball exists (B.8.2). |
| **T7** (2026-Q4) | **B.6 `event-driven-eu`** (NATS JetStream + Temporal + AsyncAPI 3.1) ; **B.7 `ai-native-rag`** (pgvector + LLM gateway + MCP servers + Qwik streaming) ; K.1 / K.2 / K.4 / K.5 (Hermes-Async, Pythia, Iris-Web, Themis agents) | `XL` | Medium — DBOS-rs maturity, MCP evolving, Connect-Dart still community-only |
| **T8** (2027-Q1) | **B.9 `mobile-only` → `mobile-pwa-first`** (web-pwa Qwik subfolder + Bloc reinforcement + decision tree) ; **B.3 `rust-cli-tui`** (cargo-dist signed releases, multi-channel distribution) ; pedagogy C.2–C.5 (walkthrough, anti-patterns, comparison matrix, migration guide). **F.3 (release script subshell isolation + 2FA OTP) pulled forward to T5, delivered 2026-05-12 via `f3-release-script-fix`.** | `L` to `XL` | Low |
| **T9+** | G.* (Forge Guardian GitHub App, VSCode extension, pre-commit hook, Linear/Jira sync, generic CLI wrapper) ; H.* (multi-level constitution, opt-in telemetry, compliance reports OWASP ASVS / ISO 27001 / SOC 2, multi-tenant Claude Code, dashboard) ; E.3 / E.4 (retroactive dog-fooding) | `XL` | — |

### Phase 3 Items — detail

| Module | Capability                                                                                                                                                                                                  | Source                          | Effort |
|--------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|---------------------------------|--------|
| P-1    | **Done 2026-05-04** via `t4-adr-ratification` — consolidated single archived change rather than 10 separate ADR changes. Article V audit trail preserved via 35 ADDED FRs in `.forge/specs/adr-ratification.md` (10× `FR-T4-ADR-*` + 6× `FR-T4-STD-*` + 5× `FR-T4-LC-*` + 2× `FR-T4-SCH-*` + ...). Rationale : keeping the 10 ADRs in one change avoids cross-referencing churn ; subsequent partial amendments can still be tracked at FR granularity. | ARCH §5                         | `M` (delivered) |
| P-2 / J.1–J.6 | **Done 2026-05-04** via `t4-adr-ratification` — six versioned `.forge/standards/*.yaml` v1.0.0 shipped (`transport`, `state-management`, `observability`, `orchestration`, `identity`, `persistence`) with `forbidden:` lists, `enforcement:`, `expires_at:` frontmatter, ratified into `index.yml` § "T.4 ratifications" block. **Still pending : J.7 (`validate-standards-yaml.sh` linter)** and **J.8 (Janus forbidden-list enforcement rules)** — both deferred to T5. | ARCH §12.1                      | `M` (partial — J.7/J.8 in T5) |
| P-3    | **Done 2026-05-04** via `t4-adr-ratification` — `global/standards-lifecycle.md` standard + `.forge/standards/REVIEW.md` append-only ledger ; structural exemptions wired (`transport.yaml` and `state-management.yaml` flagged `expires_at: never` + `exception_constitutional: true` per ADR-006/ADR-009) ; ledger seed entry for the six v1.0.0 standards (4 with `Next review due: 2027-05-04`, 2 with `never (structural)`). Themis agent automation deferred to K.5 (T7). | ARCH §12.6                      | `S` (delivered) |
| P-4 / I.1 | **Done 2026-05-04** via `t4-adr-ratification` — `.forge/schemas/compliance-tier.schema.json` (T1/T2/T3 enum) + `.forge/schemas/archetype.schema.json` **v2** (`full-stack-monorepo`, `mobile-pwa-first`, `event-driven-eu`, `ai-native-rag`, `rust-cli-tui` + `mobile-only` deprecated legacy alias for v0.3.0 B.4 adopters until B.9 in T8) ; dispatch-table.yml updated with `flutter-firebase` removal note. | ARCH §10                        | `S` (delivered) |
| B.8    | **Flagship migration `full-stack-monorepo / 1.0.0` → `2.0.0`**: Envoy Gateway / DBOS / Connect-RPC / Zitadel / Postgres+pgvector / SigNoz+OBI+Coroot / Qwik public web. **Point of no return.** Schema bump. **B.8.15 (final item) ships T5.1 Layer D** : `forge upgrade` matrix test exercising each N-1 → N pair (`full-stack-monorepo/1.0.0 → 2.0.0` critical path, plus `mobile-only/1.0.0 → mobile-pwa-first/2.0.0` once B.9 lands, plus `event-driven-eu` / `ai-native-rag` once shipped in T7). Asserts `upgrade_history` ledger + `.merge-conflicts` empty + re-runs T5.1.B smoke on upgraded tree + tests `[NEEDS MIGRATION:]` negative path. Closes the four-layer CLI Trust Harness opened in T5.1. | ARCH §11 + §6.1 + `docs/new-archetypes-plan.md` §4.2 B.8.15 | `XL`   |
| B.9    | **`mobile-only` → `mobile-pwa-first`**: add `web-pwa/` Qwik subfolder + service worker + web push, default decision tree (PWA Android+desktop, native iOS fallback if push critical)                       | ARCH §6.3                       | `L`    |
| B.6    | **New archetype `event-driven-eu`**: Rust + NATS JetStream + Temporal (justified here) + AsyncAPI 3.1 + Postgres event store ; reference project `examples/forge-eda-example/`                              | ARCH §3.3 + §6.4                | `XL`   |
| B.7    | **New archetype `ai-native-rag`**: Rust + Postgres+pgvector 0.8 + LLM gateway (Mistral-EU / vLLM self-host T3) + MCP servers + Qwik streaming UI ; reference project `examples/forge-rag-example/`           | ARCH §3.3 + §6.5                | `XL`   |
| B.3    | Archetype `rust-cli-tui` with `cargo-dist`, signed releases (codesign macOS / Authenticode Windows / gpg), SBOM SPDX, multi-channel distribution (Homebrew / Scoop / cargo binstall / AUR / Nix flake)       | Audit Module B.3 (kept)         | `XL`   |
| K.1–K.5 | Five new agents (Hermes-Async, Pythia, Demeter, Iris-Web, Themis) + edits cross-agents (Janus / Atlas / Apollo / Vulcan / Hermes-API / Argus / Sentinel / Panoptes / Aegis / Heracles)                     | ARCH §9.1 + §9.2                | `L`    |
| I.1–I.6 | Compliance EU graded T1/T2/T3: schemas, `global/compliance-tiers.md`, linter rule (T3 forbids US managed services), `forge-compliance.yml` workflow, `.forge/compliance/` (NIS2 / DORA / CRA / AI Act). **I.2 + I.3 + I.5 + I.6 Done 2026-05-12** ; NIS2/DORA/CRA/AI Act regulatory deadline artefacts still pending Themis (K.5, T7+).        | ARCH §10                        | `L`    |
| C.2–C.5 | Pedagogy: walkthrough "first 30 minutes", anti-patterns gallery, comparison matrix vs BMAD/SpecKit/Agent OS V3/Superpowers, migration guide                                                                  | Audit Module C (carry-over)     | `M`    |
| F.3    | **Done 2026-05-12** via `f3-release-script-fix`. Renamed `scripts/release-v0.3.0.sh` → `scripts/release.sh` (generic, accepts `--version X.Y.Z`) ; refactored `run()` helper to wrap `eval` in a subshell so cumulative `cd cli` operations no longer leak into the parent shell (bug 1) ; added `--otp <6-digits>` flag + interactive `read -rsp` TTY fallback + `$NPM_OTP` env-var fallback (ADR-F3-004) forwarded to `npm publish --otp="$OTP"` (bug 2). OTP value never echoed ; dry-run trace redacts to `<redacted>`. Old script deleted, no symlink (ADR-F3-001). `GOVERNANCE.md § Release Process` updated. 10/10 L1 tests `f3.test.sh` ; +1 L2 opt-in via `FORGE_F3_LIVE=1` exercising the dry-run redaction. `forge-ci.yml` ≤ 300 lines (286). Pulled forward from T8. | v0.3.0 release post-mortem      | `S` (delivered) |
| G.1–G.7 | GitHub Actions for downstream projects, `forge-hooks` pre-commit, Forge Guardian GitHub App, Linear/Jira sync, OpenAPI export, generic CLI wrapper, VSCode extension                                       | Audit Module G                  | `L` to `XL` |
| H.1–H.6 | Multi-level constitution, registry of standards, opt-in telemetry, compliance reports, multi-tenant Claude Code teams, web dashboard                                                                          | Audit Module H                  | `XL`   |

> **Migration safety net** : `forge upgrade` (A.7) already ships the BASE
> recovery via committed snapshot tarballs. The legacy
> `full-stack-monorepo / 1.0.0` snapshot stays in
> `.forge/scaffold-snapshots/` so existing adopters can stay on Kong + Temporal +
> REST-bridge until **2027-Q1** (T8) at the earliest, when the legacy compat is
> formally deprecated.

## Phase 4 (Later — 2027-Q2 and beyond)

Longer-horizon ideas, not committed. These exist to capture direction and
ensure Phase 3 architecture does not foreclose on them.

### Phase 4 Ideas

| Idea                                                                                                | Why It Matters                                                                                                | Why It's Later                                                                 |
|-----------------------------------------------------------------------------------------------------|----------------------------------------------------------------------------------------------------------------|--------------------------------------------------------------------------------|
| Archetype `data-intensive` (ClickHouse / Citus sharded / time-series + analytic workloads)         | Covers a niche left open by `full-stack-monorepo` — analytic-first products                                    | Volontairement écarté en Phase 3 faute de demande explicite (ARCH §14 caveat 9)|
| Archetype Compose Multiplatform (Kotlin) as parallel to Flutter                                    | Tech Radar Vol 33 places Compose Multiplatform on the rise (Netflix, McDonald's prod)                         | ARCH §14 caveat 5 — Forge cible 2027-2028                                       |
| **Multi-vendor agent core** (Module L.1) — single source of truth `.forge/agent-core/` (27 personas + 19 commands + 3 skills as vendor-neutral YAML) emitted into per-vendor connectors for **Claude Code, Codex CLI, Antigravity CLI, Cursor**, etc. Claude stays first-class (zero regression) ; other vendors via capability-tiered graceful degradation. Supersedes the earlier `forge-cli provider-neutral wrapper` framing. | Unlocks audiences locked out of Claude Code while keeping the 58 agnostic `.forge/` scripts unchanged ; collapses the per-vendor maintenance tax to a single core edit + regenerate. | Audit Module G.6, elaborated — requires stable internal contracts first ; targets **T9+** after the 5 archetypes stabilise. Feasibility: `docs/new-archetypes-plan.md` §0.11 + §1.4 (L.1). |
| GitHub App "Forge Guardian" that posts constitutional-compliance status on PRs                      | Automates the gate without requiring contributor installation                                                  | Audit Module G.3 — depends on stable `constitution-linter.sh` coverage         |
| VSCode extension with FR-ID autocomplete and spec-delta previews                                    | Reduces friction for contributors who never touch the CLI                                                      | Audit Module G.7                                                               |
| Linear / Jira plugin bidirectional sync                                                             | Bridges Forge artifacts to enterprise PM stacks                                                                | Audit Module G.4                                                               |
| Multi-level constitution (org + project + team amendments)                                          | Blocks enterprise adoption until delivered                                                                     | Audit Module H.1                                                               |
| Opt-in telemetry to understand which gates block most and which agents fire                        | Can't improve a framework whose usage is invisible                                                             | Audit Module H.3 — requires privacy review and opt-in UX                       |
| Compliance reports mapping constitutional articles to OWASP ASVS / ISO 27001 / SOC 2                | Unlocks regulated-industry procurement                                                                          | Audit Module H.4                                                               |
| Retroactive `.forge/changes/001-initial-scaffolding/` documenting the framework's own construction  | Dog-fooding proof + pedagogy                                                                                  | Audit Module E.3 — low urgency vs. external adoption blockers                  |

## Key Milestones

| Milestone                                                  | Target Date | Success Criteria                                                                                                                                                                                                                                                  | Status                        |
|------------------------------------------------------------|-------------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-------------------------------|
| Alpha: OSS release + Forge dog-foods itself                | 2026-06-30  | Apache 2.0 LICENSE + NOTICE; mission + roadmap filled; settings.json cleaned; shell installer on macOS + Linux; `@sdd-forge/cli` on npm; Docker image                                                                                                              | Complete (2026-04-21, v0.2.1) |
| Beta: flagship + second archetype + reference + governance | 2026-09-30  | `full-stack-monorepo / 1.0.0` + `mobile-only / 1.0.0` archetypes scaffoldable via `/forge:init` ; reference project `examples/forge-fsm-example/` ; `forge-ci.yml` live ; `forge upgrade` ; governance BDFL-with-fallback ; T3 robustness (F.1+F.2+F.4) all closed | Complete (2026-05-02, v0.3.0) |
| **ADR ratification (architecture target)**                 | 2026-09-30  | 10 ADRs of `docs/ARCHITECTURE-TARGET.md` archived as `.forge/changes/` ; six versioned `.forge/standards/*.yaml` ; compliance T1/T2/T3 schemas ; Demeter agent ; Phase 1 ARCH (OTel+Connect additive) shipped                                                       | **Partial (2026-05-04 / 06 / 08 / 10 ×2 / 12 ×6)** : T4 methodology done via `t4-adr-ratification` (PR #2). T5 Phase 1 Connect codegen archived 2026-05-06 via `t5-connect-codegen` (PR #3). J.7 standards YAML automated enforcement archived 2026-05-08 via `j7-validate-standards-yaml` (PR #4 merged). T5 OTel + OBI + Coroot stack templates archived 2026-05-10 via `t5-otel-stack` (PR #5 merged). J.8 Janus refusal rules + `--eu-tier` flag + CycloneDX SBOM archived 2026-05-10 via `j8-janus-rules` (PR #6 merged). Demeter agent (K.3 + I.4) archived 2026-05-12 via `k3-demeter` (PR #7 merged ; persona + scanner + standard + Janus delta + 22/22 tests). Flutter OTel standard v1.1.0 realign on real Workiva `opentelemetry: 0.18.11` archived 2026-05-12 via `t5-otel-dart-api-realign` (PR #8 merged ; Q-004 resolution ; 9 fabricated symbols removed + 7 verified symbols added ; 12/12 L1 tests). T5 OTel App SDK instrumentation (Phase B) archived 2026-05-12 via `t5-otel-app` (PR #9 merged ; depends on `t5-otel-dart-api-realign` ; 18/18 GREEN at `--level 1,2`). I.2 compliance tiers standard archived 2026-05-12 via `i2-compliance-tiers` (`global/compliance-tiers.md` v1.0.0 437 LOC + index entry + REVIEW birth + `docs/COMPLIANCE.md` + 14/14 L1 tests ; resolves K.3 forward-pointer ; unblocks I.3 / I.5 / I.6). I.6 compliance artefacts bundle archived 2026-05-12 via `i6-compliance-artefacts` (deterministic `.tgz` bundle generator `.forge/scripts/compliance/bundle.sh` + DPA template + standard `global/compliance-artefacts-bundle.md` v1.0.0 + 16/16 tests `i6.test.sh --level 1,2` ; `SOURCE_DATE_EPOCH` byte-identical determinism ; forward-stable layout for Themis-territory artefacts). I.3 T3-Forbidden Components linter rule archived 2026-05-12 via `i3-t3-forbidden-linter` (`constitution-linter.sh::ADR-I3-001` section + standard `global/forbidden-components-rules.md` v1.0.0 with 10 seed rules `T3-RULE-001..010` + tier-scaled severity T1/T2 warn → T3 fail + 14/14 L1 tests `i3.test.sh` ; resolves I.2 `linter_rule:` forward-pointer ; unblocks I.5). I.5 reusable compliance workflow archived 2026-05-12 via `i5-compliance-workflow` (`.github/workflows/forge-compliance.yml` 158 LOC `on: workflow_call:` orchestrant Demeter + linter + SBOM + bundle ; 3 inputs / 1 output ; upload `.tgz` via `actions/upload-artifact@v4` ; standard `global/forge-compliance-workflow.md` v1.0.0 284 LOC 7 H2 + 4 MUST NOT + 8 triggers ; 17/17 tests `i5.test.sh --level 1,2` ; trois ADRs ADR-I5-CW-001..003 ; resolves I.6 `Consumption protocol` forward-pointer). T5 OTel Phase D live-run collector contract validation archived 2026-05-12 via `t5-otel-live-run` (hermetic Python-stdlib fake OTLP collector + smoke driver + 2 golden captures + BDD feature ; 9/9 GREEN at `--level 1,2` ; Envoy live-run deferred to T6 / B.8). NIS2/DORA/CRA/AI Act regulatory-deadline artefacts (Themis K.5, T7+) still pending. |
| **v0.4.0: flagship 2.0.0 — point of no return**            | 2026-12-31  | `full-stack-monorepo / 2.0.0` (Envoy + DBOS + Connect + Zitadel + Postgres+pgvector + SigNoz+OBI+Coroot + Qwik public web) ; `forge upgrade` migrates 1.0.0 → 2.0.0 ; CI rollback runbook documented ; legacy 1.0.0 still supported as compat                       | Not Started (T6)              |
| **v0.5.0: five archetypes + EU compliance**                | 2027-Q1     | `event-driven-eu`, `ai-native-rag`, `mobile-pwa-first`, `rust-cli-tui` all delivered ; pedagogy C.2–C.5 shipped ; constitution-linter coverage ≥ 90 % articles ; 5+ projects using Forge publicly ; T3 SecNumCloud profile validated on at least one archetype     | Not Started (T7–T8)           |
| **v1.0: enterprise readiness**                             | 2027-Q3     | Forge Guardian GitHub App ; multi-level constitution ; opt-in telemetry ; compliance reports OWASP ASVS / ISO 27001 / SOC 2 ; 20+ projects using Forge publicly                                                                                                    | Not Started (T9+)             |

## Deprioritized / Parked

- **Archetype `flutter-firebase`** (originally Audit Module B.2). **Removed
  from the roadmap** as of 2026-05-04 following `docs/ARCHITECTURE-TARGET.md`
  ADR-007: Schrems II + CLOUD Act make Firebase incompatible with Forge's
  EU/premium positioning. Adopters who insist on Firebase keep the `default`
  file-copy archetype as a starting point and add Firebase themselves. A
  potential future `flutter-baas-eu` archetype (Supabase EU self-host or
  Appwrite) is not committed and waits on demonstrated demand.
- **Language extension to TypeScript / Node / Python / Go / Java / Swift**.
  Deliberately set aside. Forge targets Flutter and Rust as a premium
  positioning — **five archetypes** pushed to the state of the art beat seven
  archetypes held at mediocrity. Revisit only if a credible maintainer for
  a new language-arch emerges from the community.
- **Visual spec editor (web UI)**. File-based markdown is faster to ship,
  easier to diff, and sufficient for the target audience. Parked pending
  clear demand from a contributor population that refuses the CLI workflow.
- **Forge Cloud (hosted spec history, dashboards)**. Would require a
  commercial structure Forge does not currently have. Parked indefinitely;
  governance model (D.5) is now in place but commercial mandate is not.
- **Auto-generation of feature code from specs**. Contrary to the core
  premise — Forge enforces process, not output. Parked permanently unless
  the positioning changes.
- **GraphQL Federation** as a default transport. Rejected by ARCH ADR-009
  for monorepo scale ; acceptable only if a polyglot non-Connect client
  forces it (`opinion d'architecte non sourcée`).
- **Datadog / AWS Secrets Manager / Firebase Auth / Vertex AI / Bedrock**
  as default integrations. Rejected by ARCH §10.2 (CLOUD Act) for EU-strict
  archetypes. Adopters in the US can substitute manually but no Forge
  template ships them as default.
